---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go.
disable-model-invocation: true
---

Run a `/grilling` session, using the `/domain-modeling` skill.

Maintain a **reference doc** alongside the interview — a single page at `temp/<task>-reference.html` showing the whole state of the session with the open question loudest, regenerated by a fork subagent after every question. Follow [REFERENCE-DOC.md](./REFERENCE-DOC.md); start from [TEMPLATE.html](./TEMPLATE.html).
