---
phase: 01-foundation-curator
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .claude/skills/wiki-rules/SKILL.md
  - .claude/agents/wiki-curator.md
  - .claude/skills/wiki-rules/.gitkeep
  - .claude/agents/.gitkeep
autonomous: true
requirements:
  - DIGS-04
  - DIGS-05
  - DIGS-06
  - DIGS-07
  - DIGS-13
tags:
  - claude-code
  - skills
  - subagents
  - wiki-rules
must_haves:
  truths:
    - "A wiki-rules skill exists and instructs the curator to re-read wiki/Rules.md fresh at activation (NOT mirror it)"
    - "A wiki-curator subagent exists with frontmatter `tools: [Read, Write, Edit, Glob, Grep]`"
    - "The curator's system prompt encodes routing per Rules.md §2 (5 canonical categories), template per Rules.md §3, kebab-case per §5, ≤25-word summaries per §3, and never-modify-Rules.md per DIGS-13/D-16"
    - "Hybrid override (D-01) is explicit in the curator prompt: handle is default, content can override, override surfaced in plan"
    - "Same-concept detection uses filename + title + tag overlap per D-04 (no embeddings — A10 anti-feature)"
    - "Non-fork fallback path is documented per D-13 (CLAUDE_CODE_FORK_SUBAGENT=0 case)"
    - "Curator path-validates every write target to ensure it lives under wiki/ (and never wiki/Rules.md or wiki/_templates/)"
    - "The curator plan output requires a single plan-level approval — no per-row confirmation prompts (D-03)"
  artifacts:
    - path: ".claude/skills/wiki-rules/SKILL.md"
      provides: "Thin pointer skill that re-reads wiki/Rules.md fresh at activation"
      contains: "wiki/Rules.md"
      min_lines: 15
    - path: ".claude/agents/wiki-curator.md"
      provides: "Curator subagent definition + system prompt"
      contains: "tools:"
      min_lines: 80
  key_links:
    - from: ".claude/agents/wiki-curator.md frontmatter"
      to: "skills:"
      via: "preload list including wiki-rules"
      pattern: "skills:.*wiki-rules"
    - from: ".claude/agents/wiki-curator.md system prompt"
      to: "wiki/Rules.md"
      via: "instruction to read it fresh at activation"
      pattern: "wiki/Rules\\.md"
    - from: ".claude/skills/wiki-rules/SKILL.md"
      to: "wiki/Rules.md"
      via: "Read tool invocation in skill body (NOT a copy of contents)"
      pattern: "Read.*wiki/Rules\\.md|cat.*wiki/Rules\\.md"
---

<objective>
Build the `wiki-rules` skill (a thin pointer that re-reads `wiki/Rules.md` at activation, NOT a mirror) and the `wiki-curator` subagent definition (frontmatter + system prompt). The curator system prompt encodes the routing/edit/lint protocol that Plan 03's digest skill will invoke via `context: fork`.

Purpose: The curator is the riskiest reasoning component (most rules, hardest to validate). Its system prompt is where routing intelligence, Rules.md compliance, hybrid-override behavior, same-concept detection, and path safety live. Wave 2 plans 02 and 03 are parallel because they own disjoint files; the curator (this plan) and the digest skill (Plan 03) are wired together by string reference (`agent: wiki-curator`).

Output: Two markdown files (`SKILL.md` for wiki-rules, `wiki-curator.md` for the agent) with concrete frontmatter and system-prompt content that respects all of D-01, D-02, D-04, D-13, D-16 from CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-curator/01-CONTEXT.md
@.planning/phases/01-foundation-curator/01-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@wiki/Rules.md
@wiki/_templates/note.md

<interfaces>
<!-- Claude Code subagent frontmatter (verified in research/STACK.md and research/ARCHITECTURE.md): -->
<!--   name: <slug>                  — required, becomes the agent's identity -->
<!--   description: <text>           — required, used for selection/discovery -->
<!--   tools: [Tool, Tool, ...]      — comma-separated allowlist (per STACK.md sketch) -->
<!--   model: inherit                — use parent's model -->
<!--   skills: [skill-name, ...]     — preload list (each name resolves to .claude/skills/<name>/SKILL.md) -->

<!-- Claude Code skill (SKILL.md) frontmatter: -->
<!--   name: <slug>                  — required, becomes /<slug> -->
<!--   description: <text>           — required, used for model invocation -->
<!--   user-invocable: false         — for wiki-rules: not a user-facing slash command -->
<!--   disable-model-invocation:false — wiki-rules must be invocable so curator can preload it -->
<!--   allowed-tools: <Tool list>    — pre-approved tool list -->

<!-- Hybrid override (D-01, D-02): handle = default route; content can override; -->
<!-- override surfaced as explicit line in plan; no "skip on disagreement" path. -->

<!-- Same-concept detection (D-04): filename + title + tag overlap. NO embeddings (A10). -->
</interfaces>

<rules_md_excerpts>
<!-- Quoted from wiki/Rules.md so the executor doesn't have to re-read it for every prompt section. -->
<!-- The curator's system prompt MUST encode equivalents of these. -->

§2 Categories (5 canonical):
  ARCHITECTURE/  — system architecture, project structure, dev workflow, build/deploy, db setup
  FUNCTIONS/     — reference docs for functions/endpoints/handlers/services
  RESEARCH/      — domain math, formulas, algorithms, derivations, design notes
  SELF/          — AI/agent-facing context: memory snapshots, session summaries
  DIAGRAMS/      — workflows, mermaid diagrams, user flows

§3 Template (every filed note):
  **Summary**: ≤25 words, one sentence
  **Tags**: 2–5 lowercase hashtags
  **Created**: ISO-8601 (immutable after first write)
  **Last Updated**: ISO-8601 (changes on every edit)
  --- separator
  ## Content
  ## Related Notes (uses [[Title]] form, NOT filename)

§4 Length: hard target 1,000 words/note. Split if exceeded; splits cross-link.
§5 Filenames: kebab-case, ^[a-z0-9][a-z0-9-]*\.md$
§7 Linking: [[Note Title]] for internal, never hard-coded paths.
§8 Editing: bump Last Updated; never silently delete (replace body with deprecation pointer if obsolete).
§9 _templates/ and inbox/ are special; do not file notes there.
§10 Conflict surfacing: surface conflicts before acting.
</rules_md_excerpts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author wiki-rules SKILL.md (thin pointer, NOT a Rules.md mirror)</name>
  <files>.claude/skills/wiki-rules/SKILL.md</files>
  <read_first>
    - wiki/Rules.md (just to confirm what it contains; the skill body is a POINTER to this file, not a copy)
    - .planning/research/ARCHITECTURE.md §"Pattern: thin pointer (anti-pattern: mirror)"
    - .planning/research/STACK.md §"`.claude/skills/wiki-rules/SKILL.md` — the contract carrier" (the SKETCH; we are NOT copying its body — it shows mirroring which is the anti-pattern)
    - .planning/phases/01-foundation-curator/01-CONTEXT.md §"Established Patterns" (mentions wiki-rules is a thin pointer)
  </read_first>
  <behavior>
    The skill MUST instruct the curator to read `wiki/Rules.md` FRESH at every activation — never to rely on the skill body's content. This is anti-pattern 2 in research/ARCHITECTURE.md: mirroring Rules.md into the skill body invites drift.

    The skill body must:
    - Tell the curator to invoke its Read tool against `wiki/Rules.md` immediately on activation
    - State that the contents of that file are the AUTHORITATIVE contract for this run
    - Forbid the curator from modifying `wiki/Rules.md` (DIGS-13, D-16) — restated here so the curator sees it via skill preload, not just via agent system prompt

    Replace the existing `.claude/skills/wiki-rules/.gitkeep` by writing SKILL.md alongside (the .gitkeep can be deleted in this task or left — both are tracked).
  </behavior>
  <action>
    Use the Write tool to create `.claude/skills/wiki-rules/SKILL.md` with EXACTLY this frontmatter and a body of equivalent intent (the body's prose may be reworded but must preserve every load-bearing instruction):

    ```markdown
    ---
    name: wiki-rules
    description: Authoritative wiki conventions. Re-reads wiki/Rules.md at activation. Preloaded into the wiki-curator subagent so the contract is in context from turn 1.
    user-invocable: false
    disable-model-invocation: false
    allowed-tools: Read
    ---

    # Wiki rules contract carrier

    This skill does not contain the wiki rules. It POINTS at them. The rules live in `wiki/Rules.md`, which is the only source of truth and which the user is allowed to edit at any time. Treating any other copy as authoritative leads to drift — see research/ARCHITECTURE.md anti-pattern 2.

    ## What you must do at activation

    1. Read `wiki/Rules.md` using your Read tool. Read it in full, not partially.
    2. Treat its contents as the authoritative contract for this run. Every routing decision, every filename, every template field, every wiki-link must conform.
    3. If `wiki/Rules.md` does not exist, halt and surface the missing-contract condition to the caller. Do not invent rules.

    ## What you must NOT do

    - Do not modify `wiki/Rules.md`. Per DIGS-13 / D-16, the curator may PROPOSE rule changes in its plan output as suggestions for the user to apply manually. It must never edit Rules.md autonomously.
    - Do not assume any prior session's view of Rules.md is current. The user may have edited it between digests.
    - Do not file notes into `wiki/_templates/` or `wiki/inbox/` (Rules.md §9).

    ## Why a thin pointer instead of a mirror

    Mirroring Rules.md into this skill body would:
    - Drift silently when the user edits the real file.
    - Bloat every curator spawn with a duplicate copy of a file Read can fetch in one tool call.
    - Create two sources of truth, neither of which is unambiguously correct.

    Reading fresh costs one tool call per digest run. The cost is trivially worth the correctness guarantee.
    ```

    Note: the `.gitkeep` from Plan 01 may be left in place or removed. Git will track SKILL.md regardless.
  </action>
  <acceptance_criteria>
    - File `.claude/skills/wiki-rules/SKILL.md` exists with valid YAML frontmatter (delimited by `---`)
    - Frontmatter contains literal lines: `name: wiki-rules`, `description:`, `user-invocable: false`, `allowed-tools: Read`
    - Body contains the literal string `wiki/Rules.md` (instructing the curator to read it)
    - Body contains the substring "thin pointer" or "POINTS at" (anti-mirror framing)
    - Body explicitly forbids modifying Rules.md (mentions `DIGS-13` or `D-16`)
    - Body does NOT contain the literal contents of Rules.md §2's category table (i.e., does not contain the substring `vorp-position-multipliers` or any concrete category list — verify by ensuring the file does not include the string "ARCHITECTURE/  — system architecture")
    - File is at least 15 lines long (excluding frontmatter)
  </acceptance_criteria>
  <verify>
    <automated>test -f .claude/skills/wiki-rules/SKILL.md && grep -q "^name: wiki-rules$" .claude/skills/wiki-rules/SKILL.md && grep -q "^user-invocable: false$" .claude/skills/wiki-rules/SKILL.md && grep -q "wiki/Rules.md" .claude/skills/wiki-rules/SKILL.md && grep -qE "DIGS-13|D-16" .claude/skills/wiki-rules/SKILL.md && ! grep -q "ARCHITECTURE/  — system architecture" .claude/skills/wiki-rules/SKILL.md && [ "$(grep -v '^#' .claude/skills/wiki-rules/SKILL.md | wc -l)" -ge 15 ]</automated>
  </verify>
  <done>SKILL.md exists with correct frontmatter; body instructs fresh read of wiki/Rules.md; body forbids modifying Rules.md; body does NOT mirror Rules.md content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author wiki-curator subagent definition + system prompt</name>
  <files>.claude/agents/wiki-curator.md</files>
  <read_first>
    - wiki/Rules.md (full file — the curator's prompt must enforce these rules)
    - wiki/_templates/note.md (the schema curator must emit against)
    - .planning/phases/01-foundation-curator/01-CONTEXT.md §"Implementation Decisions" (D-01 through D-16)
    - .planning/research/ARCHITECTURE.md §"Pattern 5: Curator Proposes-Then-Applies" + §"wiki-curator subagent" (component responsibilities table)
    - .planning/research/STACK.md §".claude/agents/digest-router.md — the sub-agent" (frontmatter shape; note: the research SKETCH used name `digest-router` but CONTEXT.md and ARCHITECTURE.md settle on `wiki-curator` as the canonical name — use `wiki-curator`)
    - .planning/research/PITFALLS.md §"Pitfall 9 (duplicates)", §"Pitfall 10 (cross-link rot)", §"Pitfall 12 (Rules.md violations)"
    - .claude/skills/wiki-rules/SKILL.md (just created; the curator preloads this)
  </read_first>
  <behavior>
    The subagent definition must:
    - Use frontmatter `name: wiki-curator`, `tools: [Read, Write, Edit, Glob, Grep]`, `model: inherit`, `skills: [wiki-rules]`
    - Have a system-prompt body that encodes:
      1. Routing per Rules.md §2 (5 canonical categories): DIGS-04
      2. Template conformance per Rules.md §3: DIGS-05
      3. Kebab-case filenames per Rules.md §5: DIGS-06
      4. ≤25-word summaries per Rules.md §3: DIGS-07
      5. Never modify wiki/Rules.md (DIGS-13, D-16)
      6. Hybrid override (D-01, D-02): handle is default, content can override, override surfaced in plan, never skip
      7. Same-concept detection by filename + title + tag overlap (D-04) — no embeddings
      8. Path safety: every write target normalized to live under `wiki/`; `wiki/Rules.md` and `wiki/_templates/` are denylisted
      9. Plan-then-apply (Pattern 5): emit a markdown plan first, validate against Rules.md, only then write
      10. Non-fork fallback path documented (D-13): if `CLAUDE_CODE_FORK_SUBAGENT=0`, the digest skill calls this agent by name instead of forking
  </behavior>
  <action>
    Use the Write tool to create `.claude/agents/wiki-curator.md` with this structure (frontmatter is exact; body prose may be reworded but must preserve every numbered protocol below):

    ```markdown
    ---
    name: wiki-curator
    description: Routes inbox entries into filed wiki notes per wiki/Rules.md. Read+Write inside wiki/. Plans first, then writes after validation. Never modifies wiki/Rules.md.
    tools: Read, Write, Edit, Glob, Grep
    model: inherit
    skills:
      - wiki-rules
    ---

    # Wiki curator

    You are the wiki-curator subagent. You receive an inbox content payload (typically from `wiki/inbox/_session.md` or a fixture under `tests/fixtures/`) plus a wiki tree listing plus the contents of `wiki/Rules.md` (loaded by the wiki-rules skill at startup). Your job: produce a routing plan, validate it against Rules.md, then apply it.

    Operate only inside `wiki/`. Never touch `.claude/`, `src/`, `.planning/`, `tests/`, or any path outside `wiki/`. The two paths inside `wiki/` you may NEVER write to are `wiki/Rules.md` (the contract — DIGS-13, D-16) and `wiki/_templates/` (the schema — Rules.md §9).

    ## Protocol

    ### Step 1 — Read the inputs

    1. Confirm the wiki-rules skill has been preloaded; if not, Read `wiki/Rules.md` yourself.
    2. Glob `wiki/**/*.md` (excluding `wiki/inbox/` and `wiki/_templates/`) to enumerate existing filed notes by filename. This is your duplicate-detection index (DIGS-09; Pitfall 9 mitigation).
    3. Read each entry from the inbox payload. Inbox entries follow the handle convention: a single line starting with `@ CATEGORY::slug  •  path-or-em-dash  •  #tags` followed by a body paragraph or three.
    4. **Wiki tree fallback (W7):** If the wiki tree listing received in your input from the calling skill is empty, blank, or missing (this happens when the consumer has `disableSkillShellExecution: true` and the digest skill's bash-injected `find` produced an empty string), Glob `wiki/**/*.md` yourself excluding `wiki/inbox/` and `wiki/_templates/`, then proceed. Never proceed against an empty tree listing without verifying via Glob — silent emptiness would defeat same-concept detection.

    ### Step 2 — Decide routing per entry (DIGS-04, D-01, D-02)

    For each entry:
    1. Default route is the entry's `CATEGORY::` handle.
    2. Read the body. If it strongly disagrees with the handle (e.g., handle says FUNCTIONS but body describes a system-level boundary spanning multiple components), OVERRIDE the route. The override must be ONE of the five canonical categories (Rules.md §2): ARCHITECTURE, FUNCTIONS, RESEARCH, SELF, DIAGRAMS.
    3. Surface the override as an explicit line in the plan, e.g.: `OVERRIDE: handle says FUNCTIONS, content reads as ARCHITECTURE — routing to ARCHITECTURE because <one-sentence reason>`.
    4. Agreement is silent. Disagreement is loud.
    5. There is no "skip on disagreement" path. You always pick a route. (D-02)

    ### Step 3 — Detect same-concept conflicts (DIGS-09, D-04)

    For each entry's slug, search the wiki index from Step 1 using THREE signals (Pattern: filename + title + tag overlap; never use semantic similarity / embeddings — that's anti-feature A10):
    1. Filename match: kebab-case slug equality or near-equality (e.g., `vorp-batch-processing` matches `vorp-batch-processing.md`).
    2. Title match: read the H1 of any candidate file; compare to the entry's slug-as-title.
    3. Tag overlap: 2+ shared tags between the entry's `#tags` and the candidate note's `**Tags**:` line.

    If 2+ of these signals match: this is an EDIT, not a CREATE. Bump `Last Updated`. Do NOT create `<slug>-v2.md` or `<slug>-update.md` — that violates Rules.md §5 and Pitfall 9.

    If 0–1 signals match: this is a CREATE.

    ### Step 4 — Detect splits (DIGS-08; Rules.md §4)

    If an EDIT would push the target note's body past 1,000 words: SPLIT.
    - Each split is its own file with its own template, summary, tags.
    - Splits cross-link via `[[Title]]` (Rules.md §7) in their Related Notes sections.
    - Per D-05, D-06: when splitting, grep `wiki/**/*.md` for `[[OldTitle]]` and `[[OldTitle|alias]]`. For EACH inbound link:
      - Determine which split is most relevant based on the surrounding sentence + section heading where the link sits (D-06: per-backlink target picking).
      - Rewrite the link in place to `[[NewSplitTitle]]` (or `[[NewSplitTitle|alias]]` preserving the alias per D-07).
      - Report this in the plan as ONE summary line: "rewrite N backlinks to [[OldTitle]] across M files (per-link target chosen by surrounding context)".

    ### Step 5 — Produce the plan (DIGS-03, Pattern 5)

    Emit a markdown plan as your FIRST response. Format: a table with one row per change.

    | Action | Slug / Path | Category | Notes |
    |--------|-------------|----------|-------|
    | CREATE | `wiki/FUNCTIONS/<slug>.md` | FUNCTIONS | New note. |
    | EDIT | `wiki/FUNCTIONS/<slug>.md` | FUNCTIONS | Bump Last Updated. |
    | SPLIT | `<old> → <new1>, <new2>, <new3>` | FUNCTIONS | Inbound `[[X]]` rewrites: N across M files. |
    | OVERRIDE | (entry slug) | (new category) | Reason. |
    | RULES-PROPOSAL | (text) | — | "Suggest adding category X" — for user to apply manually. |

    Approving the plan as a whole authorizes every row in it (D-03). Do NOT prompt for per-row confirmation.

    ### Step 6 — Validate the plan against Rules.md (DIGS-05, DIGS-06, DIGS-07)

    For every CREATE/EDIT/SPLIT row:
    - Filename matches `^[a-z0-9][a-z0-9-]*\.md$` (Rules.md §5; DIGS-06).
    - Destination folder ∈ {ARCHITECTURE, FUNCTIONS, RESEARCH, SELF, DIAGRAMS} (Rules.md §2; DIGS-04).
    - Summary ≤ 25 words (Rules.md §3; DIGS-07).
    - Body ≤ 1,000 words; otherwise must be a SPLIT row (Rules.md §4; DIGS-08).
    - All template fields present (Summary, Tags, Created, Last Updated, ## Content, ## Related Notes — Rules.md §3; DIGS-05).
    - Wiki-links use `[[Title]]` form, never paths (Rules.md §7; DIGS-10).
    - No write target is `wiki/Rules.md` or anything under `wiki/_templates/` (DIGS-13, D-16, Rules.md §9).
    - No write target escapes `wiki/` (path safety).

    If any row fails validation: report the failure in the plan output and HALT before the apply step. Do not silently fix; the user may want to know.

    ### Step 7 — Apply the plan (only after validation)

    For each row:
    - CREATE: emit a new file matching `wiki/_templates/note.md` field-for-field. Created = now (ISO-8601). Last Updated = same.
    - EDIT: read the existing note, patch its Content section as needed, update Last Updated. Created field is immutable.
    - SPLIT: write the new files; update inbound `[[Old]]` references per Step 4's per-backlink decisions.
    - OVERRIDE: write to the OVERRIDDEN category, not the original.
    - RULES-PROPOSAL: do nothing in the wiki. The proposal stays in the plan output for the user.

    ### Step 8 — Post-write link audit (DIGS-11, D-08)

    After all writes:
    1. Glob all wiki notes again. Extract every `[[X]]` and `[[X|alias]]` reference.
    2. For each, verify a note with H1 `X` exists somewhere in `wiki/**/*.md`.
    3. Surface unresolved references in the digest summary as a section "Unresolved wiki-links".

    Aliased links: per D-07, the audit checks the canonical title (the part before `|`), not the alias text.

    ## Hybrid override examples (D-01)

    Good (route stays as-is, silent):
      Handle: `@ FUNCTIONS::compute-replacement-level`, body describes the function's I/O. Route: FUNCTIONS. No plan line.

    Good (override surfaced):
      Handle: `@ FUNCTIONS::auth-boundary-policy`, body describes a system-level trust boundary spanning services. Route: ARCHITECTURE. Plan line: "OVERRIDE: handle FUNCTIONS → ARCHITECTURE: content describes an inter-service trust boundary, not a single function."

    ## Non-fork fallback (D-13)

    If the digest skill cannot fork (`CLAUDE_CODE_FORK_SUBAGENT=0`), the digest skill calls you by name (`agent: wiki-curator`) and provides the inbox payload + wiki tree as text in the prompt. Your protocol is identical. Forking only changes how you are invoked, not what you do.

    ## Things you do not do

    - You do not modify `wiki/Rules.md` (DIGS-13, D-16). If you think Rules.md should change, surface a RULES-PROPOSAL row.
    - You do not touch source code, planning artifacts, hooks, or anything outside `wiki/`.
    - You do not invent categories beyond Rules.md §2's five canonical folders. If an entry doesn't fit, surface a RULES-PROPOSAL.
    - You do not use embeddings or semantic similarity for duplicate detection (anti-feature A10). Filename + title + tag overlap only.
    - You do not skip routing on disagreement (D-02). You always pick a route.
    ```

    Note: the field `tools:` accepts a comma-separated list per the STACK research sketch (`tools: Read, Write, Edit, Glob, Grep`). If your local Claude Code version requires bracket-list form, that's also acceptable — the must-haves only require the five tools to be enumerated.
  </action>
  <acceptance_criteria>
    - File `.claude/agents/wiki-curator.md` exists with valid YAML frontmatter
    - Frontmatter contains literal `name: wiki-curator`
    - Frontmatter contains all five tools: `Read`, `Write`, `Edit`, `Glob`, `Grep` (verify each appears between `tools:` and the next frontmatter key)
    - Frontmatter contains `skills:` with `wiki-rules` listed
    - Frontmatter contains `model: inherit`
    - Body explicitly references each of: `DIGS-04`, `DIGS-05`, `DIGS-06`, `DIGS-07`, `DIGS-13`, `D-01`, `D-04`, `D-13` (cross-traceability)
    - Body contains the regex `\^\[a-z0-9\]\[a-z0-9-\]\*\\\.md\$` (the kebab-case filename regex from Rules.md §5)
    - Body contains the literal string `≤ 25 words` or `<= 25 words` (DIGS-07)
    - Body contains the literal string `1,000 words` (Rules.md §4 / DIGS-08)
    - Body contains the literal string `Plan-then-apply` or `plan first` or `Pattern 5` (Pattern 5 framing)
    - Body explicitly mentions `no embeddings` or `anti-feature A10` (D-04 same-concept detection)
    - Body explicitly mentions the non-fork fallback path (`CLAUDE_CODE_FORK_SUBAGENT`)
    - Body explicitly forbids writes to `wiki/Rules.md` AND to `wiki/_templates/`
    - Body references `D-03` (plan-level approval) AND does NOT contain language that demands per-row confirmation prompts (W9)
    - File is at least 80 lines long
  </acceptance_criteria>
  <verify>
    <automated>test -f .claude/agents/wiki-curator.md && grep -q "^name: wiki-curator$" .claude/agents/wiki-curator.md && grep -qE "tools:.*Read.*Write.*Edit.*Glob.*Grep" .claude/agents/wiki-curator.md && grep -q "wiki-rules" .claude/agents/wiki-curator.md && grep -q "model: inherit" .claude/agents/wiki-curator.md && grep -q "DIGS-04" .claude/agents/wiki-curator.md && grep -q "DIGS-05" .claude/agents/wiki-curator.md && grep -q "DIGS-06" .claude/agents/wiki-curator.md && grep -q "DIGS-07" .claude/agents/wiki-curator.md && grep -q "DIGS-13" .claude/agents/wiki-curator.md && grep -q "D-01" .claude/agents/wiki-curator.md && grep -q "D-04" .claude/agents/wiki-curator.md && grep -q "D-13" .claude/agents/wiki-curator.md && grep -qE '1,000 words' .claude/agents/wiki-curator.md && grep -qE 'no embeddings|A10' .claude/agents/wiki-curator.md && grep -q 'CLAUDE_CODE_FORK_SUBAGENT' .claude/agents/wiki-curator.md && grep -q 'wiki/Rules\.md' .claude/agents/wiki-curator.md && grep -q 'wiki/_templates' .claude/agents/wiki-curator.md && grep -q "D-03" .claude/agents/wiki-curator.md && ! grep -qiE "confirm.*each|per-row|each.*entry.*confirm|prompt.*per.*entry" .claude/agents/wiki-curator.md && [ "$(wc -l < .claude/agents/wiki-curator.md)" -ge 80 ]</automated>
  </verify>
  <done>wiki-curator.md exists with correct frontmatter (5 tools, wiki-rules preload, inherit model) and a system prompt encoding all 8 protocol steps with explicit decision-ID traceability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curator subagent ↔ wiki/ filesystem | The curator has Read/Write/Edit/Glob/Grep tools scoped to wiki/. A misrouted write could corrupt notes; a path-traversal bug could escape wiki/. |
| curator ↔ wiki/Rules.md | Curator must NOT write to Rules.md (DIGS-13). The contract is the curator's only constraint; corrupting it dissolves the constraint. |
| curator ↔ wiki/_templates/ | Per Rules.md §9, _templates/ is owned by the schema; curator must not file notes there. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Path traversal in curator writes | mitigate | Curator system prompt includes Step 6 path-safety check: every write target normalized + verified to live under `wiki/`. Plan 04 acceptance gate verifies no writes occurred outside wiki/. |
| T-02-02 | Tampering | Curator writes to wiki/Rules.md | mitigate | Curator system prompt explicitly denylists `wiki/Rules.md` (DIGS-13, D-16). Validation gate (Step 6) rejects any plan row targeting Rules.md. Plan 04 verifies. |
| T-02-03 | Tampering | Curator writes to wiki/_templates/ | mitigate | Same: explicit denylist + validation gate. |
| T-02-04 | Information disclosure | wiki-rules skill leaks Rules.md content | accept | Rules.md is plain markdown the user owns. Reading it is the skill's purpose. No PII or secrets in scope. |
| T-02-05 | Tampering | Auto-rewrite of `[[X]]` backlinks scopes outside wiki/ | mitigate | Curator system prompt restricts grep+rewrite scope to `wiki/**/*.md`. Plan 04 verifies no .planning/ or src/ files were modified during a digest run. |
| T-02-06 | Spoofing | Subagent invocation by wrong name | accept | The digest skill (Plan 03) references `agent: wiki-curator` by literal name. If the user has another agent with the same name, project scope wins per STACK research; surface as install warning in Phase 4. |

ASVS Level 1. Local-only file writes scoped to wiki/. No network, no auth, no secrets.
</threat_model>

<verification>
- wiki-rules SKILL.md exists, instructs fresh Read of wiki/Rules.md, does not mirror its content
- wiki-curator.md exists with all 5 tools allowlisted, wiki-rules preloaded, inherit model
- Curator system prompt encodes routing (DIGS-04), template (DIGS-05), kebab-case (DIGS-06), summary cap (DIGS-07), no-Rules-modification (DIGS-13)
- Hybrid override (D-01, D-02) present with worked example
- Same-concept detection (D-04) present with no-embeddings stance
- Non-fork fallback (D-13) documented
- Path safety + denylist for Rules.md and _templates/ explicit
- Plan-then-apply protocol (Pattern 5, DIGS-03) documented
</verification>

<success_criteria>
- A future Claude executing the digest skill (Plan 03) and forking into wiki-curator can read these two files and have everything needed to route the fixture inbox correctly
- All 5 of this plan's requirements (DIGS-04, DIGS-05, DIGS-06, DIGS-07, DIGS-13) have explicit support in the curator prompt
- Plan 03's digest skill can reference `agent: wiki-curator` knowing the agent file exists
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-curator/01-02-SUMMARY.md` documenting:
- Final tool allowlist for the curator (any deviation from `[Read, Write, Edit, Glob, Grep]`)
- Final skill preload list (any deviation from `[wiki-rules]`)
- Notable phrasing choices in the system prompt that downstream plans should be aware of
- Anything from CONTEXT.md D-01..D-16 that needed clarification or judgment
</output>
