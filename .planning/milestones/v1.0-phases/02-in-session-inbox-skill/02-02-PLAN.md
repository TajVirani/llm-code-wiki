---
phase: 02-in-session-inbox-skill
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - wiki/inbox/_session.md
  - tests/fixtures/math.ts
autonomous: false
requirements:
  - CAPT-04

must_haves:
  truths:
    - "After turn 1 (function created), the inbox contains exactly one entry for the created function"
    - "After turn 2 (function deleted), the inbox contains zero entries for the deleted function"
    - "A /digest run after both turns produces zero filed notes for the deleted function"
    - "The deletion turn writes nothing new to the inbox (entry removal is the only change)"
  artifacts:
    - path: "wiki/inbox/_session.md"
      provides: "Post-fixture inbox with zero entries for the deleted symbol"
      contains: "# Session Inbox"
    - path: "tests/fixtures/math.ts"
      provides: "Scratch fixture file created and deleted during the acceptance loop"
  key_links:
    - from: "/inbox-update"
      to: "wiki/inbox/_session.md"
      via: "Edit tool call during skill invocation"
      pattern: "^@ FUNCTIONS::add"
    - from: "wiki/inbox/_session.md"
      to: "/digest"
      via: "curator reads inbox at digest time"
      pattern: "grep -c '^@ ' wiki/inbox/_session.md"
---

<objective>
Validate the inbox-update skill end-to-end through the locked CAPT-04 release-blocker fixture: create a function in turn 1, delete it in turn 2, verify the inbox and digest both produce zero evidence of the deleted function.

Purpose: CAPT-04 is the single locked validation criterion for Phase 2. This plan is the acceptance gate — Phase 2 is not done until this fixture passes.

Output: Evidence that the 1+1-then-deleted case works correctly: inbox contains zero entries for the deleted symbol, and digest confirms zero filed notes.
</objective>

<execution_context>
@/mnt/f/.claude/get-shit-done/workflows/execute-plan.md
@/mnt/f/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-in-session-inbox-skill/02-CONTEXT.md
@.planning/phases/02-in-session-inbox-skill/02-01-SUMMARY.md
@.claude/skills/inbox-update/SKILL.md
@.claude/agents/wiki-curator.md

<interfaces>
<!-- Key state the executor needs going into this acceptance plan. -->

From 02-CONTEXT.md §Specific Ideas (the exact fixture scenario):
- Turn 1: Claude creates `add(a, b)` in `math.ts`. /inbox-update runs. Inbox must contain:
  `@ FUNCTIONS::add  •  tests/fixtures/math.ts  •  #function #math`
- Turn 2: Claude deletes `add`. /inbox-update runs. Inbox must contain ZERO entries for `add`.
- Post-fixture /digest: must produce ZERO filed notes for `add`.

Grep pattern for checking entry absence (per D-06 handle convention):
  `! grep -q "^@ FUNCTIONS::add" wiki/inbox/_session.md`
  OR more broadly:
  `! grep -q "^@ [A-Z]*::add" wiki/inbox/_session.md`

Entry count check:
  `grep -c "^@ " wiki/inbox/_session.md`

The digest skill path: `.claude/skills/digest/SKILL.md` → invocable as `/digest`
The digest accepts an optional inbox path argument; default is `wiki/inbox/_session.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn 1 — create the fixture function and run /inbox-update</name>
  <files>tests/fixtures/math.ts, wiki/inbox/_session.md</files>
  <read_first>
    - .claude/skills/inbox-update/SKILL.md (the skill body being exercised — read in full)
    - .planning/phases/02-in-session-inbox-skill/02-CONTEXT.md (§Specific Ideas — the exact fixture scenario)
    - wiki/inbox/_session.md (confirm zero-entry state before starting)
  </read_first>
  <action>
This task simulates Turn 1 of the fixture loop. Execute it as a single coherent unit:

**Step 1 — Verify inbox is in zero-entry state:**
Read `wiki/inbox/_session.md`. Confirm it has the standard header and zero `^@ ` entries. If entries exist from a prior run, wipe the file back to the zero-entry header before proceeding.

**Step 2 — Create the fixture function:**
Write `tests/fixtures/math.ts` with the following content:

```typescript
// Fixture file for Phase 2 CAPT-04 acceptance test.
// This file is created in Turn 1 and deleted in Turn 2.

/**
 * Adds two numbers.
 * @param a First operand
 * @param b Second operand
 * @returns a + b
 */
export function add(a: number, b: number): number {
  return a + b;
}
```

**Step 3 — Invoke /inbox-update (follow the skill body):**
Now follow the `/inbox-update` skill body at `.claude/skills/inbox-update/SKILL.md` exactly, as if the Stop hook had just fired after the Write of `tests/fixtures/math.ts`. Concretely:

1. Build the scratch-list: this turn created `tests/fixtures/math.ts` containing the `add` function.
2. Run the no-op check: scratch-list is NOT empty (one file created) — proceed.
3. Reconcile: no existing `^@ FUNCTIONS::add` entry in the inbox. Append the new entry:

```
@ FUNCTIONS::add  •  tests/fixtures/math.ts  •  #function #math
Adds two numbers. Returns a + b. Accepts two numeric operands; no error handling for non-numeric input.
```

4. Check full-sweep trigger: entry count will be 1, which is well below 50 — no full sweep needed.
5. Write the entry to `wiki/inbox/_session.md` using Edit.

**Expected post-Turn-1 inbox state:**
```
# Session Inbox

**Status**: live session state
**Purpose**: state-of-the-world mirror ...

---

@ FUNCTIONS::add  •  tests/fixtures/math.ts  •  #function #math
Adds two numbers. Returns a + b. Accepts two numeric operands; no error handling for non-numeric input.
```
  </action>
  <verify>
    <automated>grep -q "^@ FUNCTIONS::add" /mnt/f/Projects/llm-code-wiki/wiki/inbox/_session.md && [ "$(grep -c "^@ " /mnt/f/Projects/llm-code-wiki/wiki/inbox/_session.md)" -eq 1 ] && grep -q "tests/fixtures/math.ts" /mnt/f/Projects/llm-code-wiki/wiki/inbox/_session.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^@ FUNCTIONS::add" wiki/inbox/_session.md` exits 0 (entry present after turn 1)
    - `grep -c "^@ " wiki/inbox/_session.md` returns 1 (exactly one entry — no extras)
    - `grep -q "tests/fixtures/math.ts" wiki/inbox/_session.md` exits 0 (path field correct)
    - `test -f tests/fixtures/math.ts` exits 0 (fixture file exists)
    - Entry body is present-tense description of what the function does (not "Added X" — that would be chronological log anti-pattern)
  </acceptance_criteria>
  <done>Inbox contains exactly one entry for @ FUNCTIONS::add with the correct path field. math.ts exists.</done>
</task>

<task type="auto">
  <name>Task 2: Turn 2 — delete the fixture function and run /inbox-update</name>
  <files>tests/fixtures/math.ts, wiki/inbox/_session.md</files>
  <read_first>
    - wiki/inbox/_session.md (current state — should have one @ FUNCTIONS::add entry from Task 1)
    - .claude/skills/inbox-update/SKILL.md (the skill body — specifically the prune-first reconcile step)
    - .planning/phases/02-in-session-inbox-skill/02-CONTEXT.md (§Specific Ideas — what "zero entries for add" means)
  </read_first>
  <action>
This task simulates Turn 2 of the fixture loop. The user has asked Claude to delete the `add` function. Execute as a single coherent unit:

**Step 1 — Delete the fixture file:**
Delete `tests/fixtures/math.ts`. Use the Bash tool: `rm tests/fixtures/math.ts`. Confirm deletion.

**Step 2 — Invoke /inbox-update (follow the skill body for a deletion turn):**
Follow `.claude/skills/inbox-update/SKILL.md` exactly:

1. Build the scratch-list: this turn deleted `tests/fixtures/math.ts` (containing the `add` function). Scratch-list: `deleted: tests/fixtures/math.ts`.
2. No-op check: scratch-list is NOT empty — proceed.
3. Reconcile — prune first:
   - Run `grep -n "^@ [A-Z]*::add" wiki/inbox/_session.md` (or equivalent) to find the entry.
   - Remove the entire entry block (handle line + body lines) using Edit.
4. No new entries to add (the scratch-list only contains a deletion).
5. Full-sweep trigger: entry count will now be 0, well below 50 — no full sweep.

**Expected post-Turn-2 inbox state:**
```
# Session Inbox

**Status**: live session state
**Purpose**: state-of-the-world mirror ...

---
```

Zero entries. The `^@ FUNCTIONS::add` entry is completely gone. No "Deleted X" or "Removed add" line — that would be a chronological log (A8 anti-pattern). The inbox is state-of-the-world and the function no longer exists; therefore no entry exists.
  </action>
  <verify>
    <automated>! grep -q "^@ FUNCTIONS::add" /mnt/f/Projects/llm-code-wiki/wiki/inbox/_session.md && [ "$(grep -c "^@ " /mnt/f/Projects/llm-code-wiki/wiki/inbox/_session.md)" -eq 0 ] && ! test -f /mnt/f/Projects/llm-code-wiki/tests/fixtures/math.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q "^@ FUNCTIONS::add" wiki/inbox/_session.md` exits 0 (entry ABSENT after turn 2)
    - `! grep -q "^@ [A-Z]*::add" wiki/inbox/_session.md` exits 0 (no add entry under any category)
    - `grep -c "^@ " wiki/inbox/_session.md` returns 0 (inbox completely clean)
    - `! test -f tests/fixtures/math.ts` exits 0 (fixture file deleted)
    - Inbox does NOT contain "Deleted", "Removed", "add was deleted" or any chronological record of the deletion (that would be the A8 anti-pattern)
  </acceptance_criteria>
  <done>Inbox contains zero entries for add. The file is deleted. The inbox is in zero-entry state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies the fixture loop and optionally runs /digest</name>
  <what-built>
    - Task 1 created tests/fixtures/math.ts and invoked /inbox-update → inbox gained one @ FUNCTIONS::add entry
    - Task 2 deleted tests/fixtures/math.ts and invoked /inbox-update → inbox was pruned to zero entries
    - Both automated verify checks passed (the grep gates above)
  </what-built>
  <how-to-verify>
    **Step 1 — Confirm the inbox is clean:**
    ```bash
    grep -c "^@ " wiki/inbox/_session.md
    # Expected: 0
    ```

    **Step 2 — Confirm the fixture file is gone:**
    ```bash
    test -f tests/fixtures/math.ts && echo "EXISTS — FAIL" || echo "GONE — PASS"
    # Expected: GONE — PASS
    ```

    **Step 3 (optional but recommended) — Run /digest to confirm zero filed notes for add:**
    Run `/digest` in the Claude Code session. The digest should:
    - Archive the empty inbox to `wiki/inbox/_archive/<timestamp>-session.md`
    - Print "Inbox is empty (0 entries); idempotent no-op."
    - NOT create any `wiki/FUNCTIONS/add.md` or related note

    Confirm:
    ```bash
    ! test -f wiki/FUNCTIONS/add.md
    # If PASS: no ghost note was created
    ```

    **Step 4 — Spot-check the skill body for quality:**
    Open `.claude/skills/inbox-update/SKILL.md` and verify:
    - First paragraph contains "derived view" and "ground truth" language
    - The 1+1-then-deleted worked example is present inline
    - Line count: `wc -l .claude/skills/inbox-update/SKILL.md` → should be ≤250

    **Step 5 — Verify no deferred ideas leaked in:**
    ```bash
    grep -i "Notable Detours" .claude/skills/inbox-update/SKILL.md && echo "FAIL — deferred section present" || echo "PASS"
    grep -i "why:" .claude/skills/inbox-update/SKILL.md && echo "WARN — rationale field (DIFF-01) present; check if required" || echo "PASS"
    ```
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass and the fixture loop validated correctly.
    Describe any failures (e.g., "add entry still present after turn 2", "digest created add.md") to trigger gap closure.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| acceptance fixture → wiki/inbox/_session.md | Fixture turns write to the inbox via the same Edit path the skill uses in production |
| wiki/inbox/_session.md → /digest → wiki/FUNCTIONS/ | Curator reads the inbox and would file notes if entries remained |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | path traversal during fixture | accept | Fixture writes only to `tests/fixtures/math.ts` (an explicit path) and reads/writes `wiki/inbox/_session.md` (fixed path). No dynamic path construction. Risk is negligible in acceptance context. |
| T-02-07 | Information Disclosure | ghost note filed at digest time | mitigate | CAPT-04 acceptance criterion: `! test -f wiki/FUNCTIONS/add.md` after digest. If the curator files a note for the deleted function, this check fails and triggers gap closure. The digest skill's empty-inbox no-op path (DIGS-12) prevents writes when inbox has 0 entries. |
| T-02-08 | Tampering | fixture leaves stale wiki artifacts | mitigate | Acceptance plan's scope is `wiki/inbox/_session.md` and `tests/fixtures/math.ts` only. The optional /digest run produces no new notes (empty inbox). Any accidental wiki writes during this plan would be visible in `git diff wiki/` and would fail review. |
</threat_model>

<verification>
Final state after all three tasks:

```bash
# 1. Inbox clean
grep -c "^@ " wiki/inbox/_session.md   # must be 0

# 2. No add entry in any form
! grep -q "^@ [A-Z]*::add" wiki/inbox/_session.md

# 3. Fixture file gone
! test -f tests/fixtures/math.ts

# 4. No ghost note filed
! test -f wiki/FUNCTIONS/add.md

# 5. Skill quality gates
grep -q "derived view" .claude/skills/inbox-update/SKILL.md
grep -q "scratch" .claude/skills/inbox-update/SKILL.md
grep -q "1+1" .claude/skills/inbox-update/SKILL.md
[ "$(wc -l < .claude/skills/inbox-update/SKILL.md)" -le 250 ]

# 6. CAPT-04 summary verdict
echo "CAPT-04 PASS: 1+1-then-deleted produces zero inbox entries and zero filed notes"
```
</verification>

<success_criteria>
CAPT-04 (locked release blocker) is satisfied when ALL of the following are true:
1. After Turn 1: inbox has exactly 1 entry — `^@ FUNCTIONS::add` with `tests/fixtures/math.ts` as path
2. After Turn 2: inbox has exactly 0 entries — `^@ FUNCTIONS::add` is completely absent
3. After optional /digest: `wiki/FUNCTIONS/add.md` does not exist
4. Human checkpoint approved (Task 3 gate passed)

All 7 Phase 2 requirement IDs are now addressed across both plans:
- CAPT-02: skill defines atomic flat entries, state-of-world, no chronological log (02-01)
- CAPT-03: `@ CATEGORY::slug` handle enables grep-prune (02-01)
- CAPT-04: 1+1-then-deleted fixture passes (02-02 ← THIS PLAN)
- CAPT-05: no-op guard + ≤250-line skill (02-01)
- CAPT-06: scratch-list evidence-grounding (02-01)
- CAPT-07: derived-view framing as first paragraph (02-01)
- LIFE-01: `wiki/inbox/_session.md` as the established write target (02-01)
</success_criteria>

<output>
After completion, create `.planning/phases/02-in-session-inbox-skill/02-02-SUMMARY.md` with:
- Per-turn results table (Turn 1 expected/observed, Turn 2 expected/observed, digest expected/observed)
- Screenshot or paste of the key grep outputs
- CAPT-04 verdict: PASS or FAIL WITH DESCRIPTION
- Any gaps deferred (none expected)
- Phase 2 closure statement if CAPT-04 passes
</output>
