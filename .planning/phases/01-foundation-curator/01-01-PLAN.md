---
phase: 01-foundation-curator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .claude/skills/digest/.gitkeep
  - .claude/skills/wiki-rules/.gitkeep
  - .claude/agents/.gitkeep
  - wiki/inbox/_session.md
  - tests/fixtures/_session-fixture.md
  - tests/fixtures/README.md
autonomous: false
requirements:
  - LIFE-02
  - LIFE-03
tags:
  - claude-code
  - hooks
  - fixtures
  - scaffolding
must_haves:
  truths:
    - "Q1 (Stop hook injection mechanism) is resolved and documented in PROJECT.md Key Decisions"
    - "Phase 3 has an unambiguous answer to: additionalContext on Stop, OR decision:block+reason"
    - "Empty .claude/skills/digest/, .claude/skills/wiki-rules/, and .claude/agents/ directories exist as commit anchors for Wave 2"
    - "A persistent placeholder inbox exists at wiki/inbox/_session.md (ROADMAP Phase 1 Success Criterion 1 deliverable) — zero entries, standard header"
    - "A hand-authored fixture inbox exists at tests/fixtures/_session-fixture.md exercising 5 specific sub-cases"
    - "The fixture references real notes in wiki/FUNCTIONS/ for the existing-note conflict sub-case"
    - "Archive-before-write ordering (LIFE-02, LIFE-03) is restated as a phase invariant in fixture README"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Q1 resolution row in Key Decisions table"
      contains: "Stop hook injection"
    - path: ".claude/skills/digest/.gitkeep"
      provides: "Wave 2 anchor for digest skill"
    - path: ".claude/skills/wiki-rules/.gitkeep"
      provides: "Wave 2 anchor for wiki-rules skill"
    - path: ".claude/agents/.gitkeep"
      provides: "Wave 2 anchor for wiki-curator subagent"
    - path: "wiki/inbox/_session.md"
      provides: "Persistent live-inbox placeholder (ROADMAP success criterion 1; D-11 path)"
      contains: "# Session Inbox"
    - path: "tests/fixtures/_session-fixture.md"
      provides: "5-sub-case fixture inbox"
      min_lines: 40
    - path: "tests/fixtures/README.md"
      provides: "Fixture sub-case index + acceptance criteria"
  key_links:
    - from: "PROJECT.md Key Decisions"
      to: "Phase 3 hook implementation"
      via: "Q1 resolution row"
      pattern: "Stop hook|additionalContext|decision.*block"
    - from: "tests/fixtures/_session-fixture.md"
      to: "wiki/FUNCTIONS/vorp-batch-processing.md"
      via: "existing-note conflict sub-case targets a real wiki note"
      pattern: "vorp-batch-processing|VORP Batch"
    - from: "wiki/inbox/_session.md"
      to: "ROADMAP Phase 1 Success Criterion 1"
      via: "persistent placeholder satisfies the deliverable"
      pattern: "# Session Inbox"
---

<objective>
Resolve Q1 (Stop hook injection mechanism: `additionalContext` vs `decision:"block" + reason`) by reading current Claude Code Hooks docs and running a one-line smoke test, document the chosen mechanism in PROJECT.md Key Decisions, scaffold empty directories that Wave 2 plans will populate, create the persistent `wiki/inbox/_session.md` placeholder (ROADMAP success criterion 1 deliverable), and hand-author the 5-sub-case fixture inbox that Plan 04 will validate against.

Purpose: Q1 gates Phase 3 — its resolution must land before any hook code is written downstream. Scaffolding directories give Wave 2 plans clean commit anchors. The persistent live-inbox placeholder satisfies ROADMAP Phase 1 Success Criterion 1 verbatim. The fixture is the test corpus that makes Phase 1 success criteria mechanically checkable.

Output: PROJECT.md updated with Q1 resolution row, three .gitkeep'd Claude Code subdirectories, persistent `wiki/inbox/_session.md` placeholder, fixture markdown file with 5 sub-cases, fixture README documenting acceptance criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-curator/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@wiki/Rules.md
@wiki/_templates/note.md
@wiki/FUNCTIONS/vorp-batch-processing.md

<interfaces>
<!-- The fixture must produce these handle-shape entries. Format from D-12 (CONTEXT.md): -->
<!--   `@ CATEGORY::slug  •  path-or-em-dash  •  #tag1 #tag2` -->
<!-- anchored at start of line for ^@ grep, blank line between entries. -->
<!-- Canonical categories from Rules.md §2: ARCHITECTURE, FUNCTIONS, RESEARCH, SELF, DIAGRAMS. -->

<!-- The five canonical wiki/_templates/note.md fields (any new note must contain these): -->
<!--   **Summary**: ≤25 words -->
<!--   **Tags**: 2–5 lowercase hashtags -->
<!--   **Created**: ISO-8601 -->
<!--   **Last Updated**: ISO-8601 -->
<!--   ## Content -->
<!--   ## Related Notes -->

<!-- Existing wiki notes the fixture's "existing-note conflict" sub-case can target (real H1 titles): -->
<!--   wiki/FUNCTIONS/vorp-batch-processing.md       — display title likely "VORP Batch Processing" -->
<!--   wiki/ARCHITECTURE/postgresql-setup.md         — display title "PostgreSQL Setup" -->
<!-- Curator's same-concept detection (D-04) uses filename + title + tag overlap. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 [BLOCKING]: Q1 — Stop hook injection mechanism, smoke test + decision</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full file — see existing "Key Decisions" table you will append to)
    - .planning/research/SUMMARY.md §"Critical Open Questions / Q1" (the contradiction summary)
    - .planning/research/STACK.md (Stack research's stance: additionalContext on Stop)
    - .planning/research/ARCHITECTURE.md §"Critical Mechanic" (Architecture research's stance: additionalContext is NOT whitelisted for Stop, must use decision:block+reason)
    - .planning/research/PITFALLS.md §"Pitfall 3" (loop hazard with decision:block)
    - .planning/phases/01-foundation-curator/01-CONTEXT.md §"Q1 resolution sub-deliverable" (D-17, D-18)
  </read_first>
  <action>
    This is a NON-CODING research deliverable. Do not write hook code yet — Phase 3 owns that. The deliverable is a documented decision in PROJECT.md.

    NOTE: The temporary settings.json write/remove for the smoke test constitutes user-authorized direct edits per the human-action checkpoint gate. The checkpoint's resume-signal is the user's authorization for this transient bypass of CLAUDE.md's GSD-only-edit constraint. The bypass is bounded to: (a) creating the temporary smoke-test hook in `.claude/settings.json`, and (b) removing that hook entry after the test. No other edits to settings.json are authorized by this checkpoint.

    Procedure (per D-17 and D-18 from CONTEXT.md, restated verbatim):

    1. **Fetch the CURRENT Claude Code Hooks docs.** Use the WebFetch tool against `https://code.claude.com/docs/en/hooks`. Do NOT rely on training-data memory of the docs. Specifically extract: which `hookSpecificOutput.hookEventName` values are documented as accepting `additionalContext`? Does `Stop` appear in that list? Quote the relevant lines verbatim.

    2. **Run a one-line smoke test.** Create a temporary Stop hook in `.claude/settings.json` that emits exactly:
       ```json
       {"hookSpecificOutput":{"hookEventName":"Stop","additionalContext":"Q1-SMOKE-TEST-MARKER"}}
       ```
       Then take a normal Claude turn. PAUSE FOR USER: ask the user (the developer) to report whether the next turn's context contains the literal string `Q1-SMOKE-TEST-MARKER`. If yes → `additionalContext` is supported on Stop. If no (or if Claude Code rejects/ignores the field, or the docs explicitly omit Stop from the whitelist) → must use `decision:"block" + reason`.

    3. **Remove the temporary hook from settings.json.** This task does not ship a working hook — that is Phase 3. After removal, settings.json should either not exist (if it didn't pre-exist) or contain no `Q1-SMOKE-TEST-MARKER` reference.

    4. **Append a new row to PROJECT.md "Key Decisions" table.** Use this exact structure (replace `<MECHANISM>` and `<EVIDENCE>`):
       ```
       | Stop hook injection mechanism = <MECHANISM> | <EVIDENCE: 1-sentence smoke-test outcome + docs quote> | Locked 2026-04-28 |
       ```
       Where `<MECHANISM>` is one of: `additionalContext` OR `decision:"block" + reason`.
       Where `<EVIDENCE>` is something like: `"Smoke test marker visible on next turn; docs at /docs/en/hooks list Stop in additionalContext-eligible events as of 2026-04-28."` OR `"Smoke test marker NOT visible; docs whitelist additionalContext only for UserPromptSubmit/SessionStart/PreToolUse/PostToolUse/UserPromptExpansion."`

    5. Update STATE.md "Locked Decisions" section to reflect Q1 resolution and remove Q1 from "Pending Decisions". Bump STATE.md `Last Updated`.

    Why we cannot defer: Phase 3 cannot start without this answer. Phase 1 itself does not need a working hook; it only needs the documented decision (D-18).

    Why human-action checkpoint: the smoke test requires the user to run a Claude turn and report what they observe in the next turn's context. The agent cannot self-verify cross-turn context injection.
  </action>
  <how-to-verify>
    1. Open .planning/PROJECT.md. Confirm a new row exists in the Key Decisions table naming the chosen mechanism (`additionalContext` or `decision:"block" + reason`) with smoke-test evidence.
    2. Confirm `.claude/settings.json` does NOT contain the temporary smoke-test hook (it was removed after the test).
    3. Confirm STATE.md "Pending Decisions" no longer lists Q1.
  </how-to-verify>
  <resume-signal>Type "Q1 resolved: additionalContext" OR "Q1 resolved: decision-block" OR describe the smoke-test outcome to continue. The signal also constitutes user authorization for the temporary settings.json bypass described in the action.</resume-signal>
  <acceptance_criteria>
    - PROJECT.md Key Decisions table contains a row matching regex: `Stop hook injection mechanism = (additionalContext|decision)`
    - PROJECT.md Key Decisions row ends with `Locked` (not `Pending`)
    - STATE.md "Pending Decisions" section no longer contains the substring "Q1"
    - .claude/settings.json either does not exist OR exists without the literal string `Q1-SMOKE-TEST-MARKER` (post-cleanup invariant)
    - The chosen mechanism is one of exactly two values: `additionalContext` or `decision:"block" + reason`
  </acceptance_criteria>
  <verify>
    <automated>grep -E "Stop hook injection mechanism = (additionalContext|decision)" .planning/PROJECT.md && ( ( ! test -f .claude/settings.json ) || ( ! grep -q 'Q1-SMOKE-TEST-MARKER' .claude/settings.json ) ) && ! grep -i "Q1.*Pending\|Pending.*Q1" .planning/STATE.md</automated>
  </verify>
  <done>PROJECT.md row added with mechanism + evidence; STATE.md updated; smoke-test hook removed (file absent OR file present with no marker); user has confirmed "Q1 resolved" with the chosen mechanism.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold .claude/ subdirectories with .gitkeep anchors + persistent wiki/inbox/_session.md placeholder</name>
  <files>.claude/skills/digest/.gitkeep, .claude/skills/wiki-rules/.gitkeep, .claude/agents/.gitkeep, wiki/inbox/_session.md</files>
  <read_first>
    - .planning/research/STACK.md §"Layout (the actual files we ship)" (the .claude/ tree shape)
    - .planning/research/ARCHITECTURE.md §"Recommended Project Structure" (confirms the same layout)
    - .planning/ROADMAP.md §"Phase 1 / Success Criteria / 1" (mandates `wiki/inbox/_session.md` as a deliverable)
    - .planning/phases/01-foundation-curator/01-CONTEXT.md §"D-11" (inbox path convention)
  </read_first>
  <action>
    Two scaffolding sub-actions. The first creates Wave-2 anchor directories. The second creates the persistent live-inbox placeholder mandated by ROADMAP Phase 1 Success Criterion 1.

    **Sub-action A — `.claude/` anchor directories:**

    Create three empty directories under `.claude/`, each containing a `.gitkeep` file so git tracks them. These are commit anchors for Wave 2 plans (02 populates wiki-rules + wiki-curator, 03 populates digest).

    Use the Write tool to create each `.gitkeep` with content exactly:
    ```
    # Anchor file. Replaced/joined by SKILL.md or agent .md in Phase 1 Wave 2.
    ```

    Create exactly these three files (and their parent directories implicitly):
    1. `.claude/skills/digest/.gitkeep`
    2. `.claude/skills/wiki-rules/.gitkeep`
    3. `.claude/agents/.gitkeep`

    Do NOT create `.claude/settings.json` here — that is Phase 3 territory (and Task 1's smoke test may have already created/removed a temporary one).

    Do NOT create `.claude/skills/inbox-update/` here — that is Phase 2 territory.

    Do NOT create `.claude/hooks/` here — that is Phase 3 territory.

    Per CONTEXT.md, Phase 1 owns ONLY: digest skill, wiki-rules skill, wiki-curator agent. Resist scope creep into other phases.

    **Sub-action B — `wiki/inbox/_session.md` persistent placeholder (ROADMAP success criterion 1):**

    ROADMAP Phase 1 Success Criterion 1 explicitly requires `wiki/inbox/_session.md` as a Phase 1 deliverable. Per D-11, this is the live-inbox path the digest skill reads by default. It must exist persistently after this plan completes — it is NOT a transient test artifact. Plan 04 will OVERWRITE this file with the fixture content for its acceptance run, but Plan 04 must be able to assume the path exists.

    Use the Write tool to create `wiki/inbox/_session.md` with EXACTLY this content (zero entries, standard header):

    ```
    # Session Inbox

    *State-of-the-world record of what's been built and decided this session. Atomic flat entries with `@ CATEGORY::slug • path • #tags` handles. Pruned on supersession; archived on digest.*

    *No entries yet.*
    ```

    The file must contain zero `^@ ` handle lines (idempotence-friendly empty state per DIGS-12). Phase 2's inbox-update skill will populate it during real session work.

    Do NOT confuse this with `tests/fixtures/_session-fixture.md` (Task 3) — they are distinct paths with distinct purposes per D-10.
  </action>
  <acceptance_criteria>
    - File `.claude/skills/digest/.gitkeep` exists and contains "Anchor file"
    - File `.claude/skills/wiki-rules/.gitkeep` exists and contains "Anchor file"
    - File `.claude/agents/.gitkeep` exists and contains "Anchor file"
    - File `wiki/inbox/_session.md` exists and starts with `# Session Inbox`
    - File `wiki/inbox/_session.md` contains zero handle lines matching `^@ `
    - Directory `.claude/skills/inbox-update/` does NOT exist (Phase 2 territory)
    - Directory `.claude/hooks/` does NOT exist (Phase 3 territory)
  </acceptance_criteria>
  <verify>
    <automated>test -f .claude/skills/digest/.gitkeep && test -f .claude/skills/wiki-rules/.gitkeep && test -f .claude/agents/.gitkeep && test -f wiki/inbox/_session.md && grep -q '^# Session Inbox' wiki/inbox/_session.md && [ "$(grep -cE '^@ ' wiki/inbox/_session.md)" -eq 0 ] && ! test -d .claude/skills/inbox-update && ! test -d .claude/hooks</automated>
  </verify>
  <done>Three .gitkeep files exist; persistent wiki/inbox/_session.md placeholder exists with zero entries; no Phase 2/3 directories created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the 5-sub-case fixture inbox + acceptance README</name>
  <files>tests/fixtures/_session-fixture.md, tests/fixtures/README.md</files>
  <read_first>
    - wiki/Rules.md (the contract — fixture entries must be designed so a CORRECT digest produces Rules-conformant notes; the fixture itself is NOT a filed note and need not conform to the template)
    - wiki/_templates/note.md (the schema curator must emit against)
    - wiki/FUNCTIONS/vorp-batch-processing.md (the real existing note for sub-case 4 to target)
    - wiki/ARCHITECTURE/postgresql-setup.md (alternate target for sub-case 4)
    - .planning/research/ARCHITECTURE.md §"Pattern 3: Atomic Flat Entries with Stable Handles" (entry shape rationale + worked example)
    - .planning/phases/01-foundation-curator/01-CONTEXT.md §"Fixture rigor (D-09, D-10)" (the 5 sub-cases enumerated)
  </read_first>
  <behavior>
    The fixture is a test corpus. It must exercise five distinct sub-cases such that:
    - Sub-case 1 (Happy path): 5 entries, exactly one per canonical category from Rules.md §2 (`ARCHITECTURE`, `FUNCTIONS`, `RESEARCH`, `SELF`, `DIAGRAMS`). Each maps cleanly to one new filed note. Tags are 2–5 lowercase hashtags.
    - Sub-case 2 (1+1-then-deleted, CAPT-04 release blocker): The deleted entry is simply ABSENT from the fixture (post-pruning state). README documents which slug WOULD have been there but isn't. Acceptance: digest produces zero filed notes for that slug.
    - Sub-case 3 (Ambiguous categorization, exercises D-01 Hybrid override): 1 entry whose `CATEGORY::` handle disagrees with content. Concrete example: handle `@ FUNCTIONS::auth-flow-overview` but body describes a system-level boundary that should route to ARCHITECTURE. Acceptance: digest plan surfaces the override.
    - Sub-case 4 (Existing-note conflict): 1 entry whose subject matches a real note already in `wiki/FUNCTIONS/`. Use slug `vorp-batch-processing` to target `wiki/FUNCTIONS/vorp-batch-processing.md`. Acceptance: digest plan reports an EDIT (not a CREATE), bumping `Last Updated`.
    - Sub-case 5 (Split-implication): 1 entry whose content (or whose target's existing content + this entry's addition) would push a note past 1,000 words. Concrete approach: use slug `vorp-batch-processing` (same as sub-case 4) and write the entry body so that adding the new content would exceed the existing note's body word count past 1,000. Acceptance: digest plan reports a SPLIT with per-backlink target picks (D-05, D-06).

    Total entries: 5 (happy) + 0 (1+1-deleted is absent) + 1 (ambiguous) + 1 (existing-conflict) + 1 (split, can be the same as existing-conflict if we make it heavy enough) = 7 or 8 entries.
  </behavior>
  <action>
    Create `tests/fixtures/_session-fixture.md` as a markdown file using the entry handle convention from D-12 (restated): `@ CATEGORY::slug  •  path-or-em-dash  •  #tag1 #tag2`, anchored at start of line, blank line between entries. Format per ARCHITECTURE research §"Pattern 3" worked example.

    The file must start with this exact header (so it parses as a session-inbox-shaped doc):
    ```
    # Session Inbox (FIXTURE)

    **Status**: hand-authored test corpus — NOT live session state
    **Purpose**: Phase 1 acceptance gate for the digest sub-agent (per .planning/phases/01-foundation-curator/01-CONTEXT.md D-09)

    ---
    ```

    Then write the entries. Concrete content (use these exactly — adjust paths/tags only as needed for plausibility against the Traxalytics fixture wiki):

    **Happy-path entries (sub-case 1) — exactly 5, one per category:**
    ```
    @ ARCHITECTURE::caching-layer-overview  •  src/lib/cache/  •  #architecture #caching #redis
    Three-tier caching strategy: in-memory LRU for hot reads, Redis for cross-process sharing,
    Postgres materialized views for cold analytics. Boundaries enforced by interface in
    src/lib/cache/index.ts; no direct Redis calls from feature code.

    @ FUNCTIONS::compute-replacement-level  •  src/lib/vorp/replacement.ts  •  #function #vorp #math
    Computes the replacement-level threshold for a given position by sorting all eligible
    players descending and taking the value at index = league_size * roster_slots[position].
    Returns NaN if the eligible pool is smaller than the slot count (caller must handle).

    @ RESEARCH::age-curve-derivation  •  —  •  #research #math #age-curve
    Derived the per-position age multipliers from a 10-season rolling regression of points-per-game
    vs. age. Forwards peak at 27, defensemen at 29, goalies at 28. Multipliers are point estimates;
    confidence intervals widen sharply outside ages 22-32.

    @ SELF::session-summary-2026-04-28  •  —  •  #self #session-summary
    Spent the session adding the cache abstraction and rederiving age curves. Open thread:
    the cache layer's eviction policy on Redis disconnect is not yet specified — will need
    a decision next session.

    @ DIAGRAMS::vorp-calculation-flow  •  —  •  #diagram #vorp #flow
    Mermaid sequence: client POST /vorp/calculate → VORPService.Compute → BatchVORPCalculator
    (if league-wide) → cache write → response. Single-player path bypasses the batch calculator.
    Documented as a swim-lane in the diagram body.
    ```

    **Sub-case 3 (ambiguous categorization — exercises D-01):**
    ```
    @ FUNCTIONS::auth-boundary-policy  •  src/auth/  •  #auth #boundary
    Defines the trust boundary between the public API surface and internal services: every
    request crosses the auth middleware exactly once, services never re-authenticate the
    same principal, and inter-service calls carry a signed service token rather than a user
    session. This is a system-level invariant about HOW services compose, not a single
    function's behavior.
    ```
    (Note: handle says FUNCTIONS but body describes a system-level boundary. Curator should override to ARCHITECTURE per D-01 Hybrid override and surface the override in the plan.)

    **Sub-case 4 + 5 combined (existing-note conflict + split-implication):**
    ```
    @ FUNCTIONS::vorp-batch-processing  •  src/lib/vorp/batch.go  •  #functions #vorp #batch #concurrency #scaling
    Extended BatchVORPCalculator with three new behaviors not in the existing wiki note:
    (1) adaptive worker pool sizing — workers scale from 4 to 16 based on queued job depth and
    available DB connection slots, with a hysteresis band to avoid thrash; (2) per-batch
    retry-with-backoff — transient DB errors retry up to 3 times with exponential backoff
    (100ms, 400ms, 1.6s) before failing the batch; (3) cross-region replication awareness —
    when the primary DB is in failover, the calculator pauses new batches and resumes from
    the last checkpoint once a healthy primary is confirmed via the readiness probe. Each of
    these adds roughly 250 words of explanation to the existing note's Content section,
    pushing the combined body well past the 1,000-word cap from Rules.md §4. The split should
    produce: vorp-batch-processing.md (kept, slimmed), vorp-batch-adaptive-workers.md (new),
    vorp-batch-retry-policy.md (new), vorp-batch-failover-handling.md (new). Backlinks from
    other notes that mention "VORP Batch Processing" in a worker-sizing context should be
    rewritten to point to the new adaptive-workers note; backlinks in a retry/error context
    should point to the new retry-policy note (per D-06 per-backlink target picking based on
    surrounding context).
    ```

    Do NOT add an entry for sub-case 2. The 1+1-deleted case is the ABSENCE of an entry — that's the whole point. Document this in the README.

    After writing the fixture, create `tests/fixtures/README.md` with this structure:
    ```
    # Phase 1 Fixture Inbox

    **Path**: `tests/fixtures/_session-fixture.md`
    **NOT** to be confused with `wiki/inbox/_session.md` (live session state, owned by Phase 2 — see D-10, D-11).

    ## Sub-cases exercised

    | Sub-case | Slug(s) | What it validates | Acceptance |
    |----------|---------|-------------------|------------|
    | 1. Happy path | caching-layer-overview, compute-replacement-level, age-curve-derivation, session-summary-2026-04-28, vorp-calculation-flow | Routing into 5 canonical categories (DIGS-04) | ≥80% land in correct category folder |
    | 2. 1+1-then-deleted (CAPT-04) | (intentionally absent: would-have-been `legacy-helper-removed`) | Curator does not invent entries from absence | 0 filed notes for any slug not in the fixture |
    | 3. Ambiguous categorization | auth-boundary-policy | Hybrid override (D-01) — handle says FUNCTIONS, content reads as ARCHITECTURE | Plan surfaces the override; routes to ARCHITECTURE |
    | 4. Existing-note conflict | vorp-batch-processing | Same-concept detection (D-04, DIGS-09) | Plan reports EDIT not CREATE for `wiki/FUNCTIONS/vorp-batch-processing.md`; `Last Updated` bumped |
    | 5. Split-implication | vorp-batch-processing | 1,000-word split logic + auto-rewrite (DIGS-08, D-05, D-06) | Plan reports split into 3+ notes; backlinks rewritten per surrounding context |

    ## Acceptance criteria (Phase 1 gate, from CONTEXT.md + ROADMAP.md success criteria)

    - [ ] ≥80% of fixture entries land in correct category folder
    - [ ] 0 Rules.md violations (filename regex `^[a-z0-9][a-z0-9-]*\.md$`, ≤25-word summary, all template fields present, wiki-link syntax `[[Title]]`)
    - [ ] Sub-case 2: digest produces zero filed notes for any slug absent from the fixture (CAPT-04 release blocker validated in Phase 1)
    - [ ] Idempotence (DIGS-12): a second digest run on a fresh post-archive empty inbox produces zero changes
    - [ ] Archive-before-write (LIFE-02, LIFE-03): a simulated mid-write crash leaves entries recoverable from `wiki/inbox/_archive/<timestamp>-session.md`
    - [ ] Aliased links `[[Old|alias]]` rewritten as `[[New|alias]]` (D-07) — verify by introducing one before split test
    - [ ] Curator never modifies `wiki/Rules.md` (D-16, DIGS-13)

    ## How Plan 04 uses this

    Plan 04 copies `_session-fixture.md` to `wiki/inbox/_session.md`, runs `/digest`, and verifies the criteria above against the resulting plan + filed notes + archive.

    ## Why this is in `tests/fixtures/` not `wiki/inbox/`

    Per D-10: `wiki/inbox/_session.md` is reserved for live session state. The fixture is checked-in test corpus that must not be mutated by ordinary digest runs.
    ```

    Note: do NOT make the fixture entries' bodies conform to the wiki note template. The fixture is INBOX content (handle line + free-form body). The CURATOR transforms inbox entries into template-conformant notes; that's the task under test.
  </action>
  <acceptance_criteria>
    - File `tests/fixtures/_session-fixture.md` exists and starts with `# Session Inbox (FIXTURE)`
    - File contains exactly 7 entry handle lines matching regex `^@ (ARCHITECTURE|FUNCTIONS|RESEARCH|SELF|DIAGRAMS)::`
    - Verify with: `grep -cE '^@ (ARCHITECTURE|FUNCTIONS|RESEARCH|SELF|DIAGRAMS)::' tests/fixtures/_session-fixture.md` returns `7`
    - Specifically contains a line matching `^@ FUNCTIONS::vorp-batch-processing` (sub-case 4+5 target)
    - Specifically contains a line matching `^@ FUNCTIONS::auth-boundary-policy` (sub-case 3, ambiguous)
    - Contains exactly 5 distinct CATEGORY values used across happy-path entries (one per canonical category)
    - File `tests/fixtures/README.md` exists, contains a row for each of sub-cases 1–5, and explicitly notes that sub-case 2 is "intentionally absent"
    - File `tests/fixtures/README.md` contains the literal string `≥80%` (acceptance gate from ROADMAP.md)
    - File `tests/fixtures/README.md` contains the literal string `CAPT-04` (release blocker reference)
    - The persistent `wiki/inbox/_session.md` placeholder from Task 2 is NOT overwritten by this task (Plan 04 owns that overwrite)
  </acceptance_criteria>
  <verify>
    <automated>test -f tests/fixtures/_session-fixture.md && test -f tests/fixtures/README.md && [ "$(grep -cE '^@ (ARCHITECTURE|FUNCTIONS|RESEARCH|SELF|DIAGRAMS)::' tests/fixtures/_session-fixture.md)" -eq 7 ] && grep -q '^@ FUNCTIONS::vorp-batch-processing' tests/fixtures/_session-fixture.md && grep -q '^@ FUNCTIONS::auth-boundary-policy' tests/fixtures/_session-fixture.md && grep -q 'CAPT-04' tests/fixtures/README.md && grep -q 'intentionally absent' tests/fixtures/README.md && test -f wiki/inbox/_session.md && [ "$(grep -cE '^@ ' wiki/inbox/_session.md)" -eq 0 ]</automated>
  </verify>
  <done>Fixture file with 7 entries (covering 5 sub-cases) and README documenting acceptance criteria both exist; sub-case 2 documented as intentional absence; live inbox placeholder from Task 2 still has zero entries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer ↔ filesystem | The agent writes files under `.planning/`, `.claude/`, `wiki/inbox/`, and `tests/fixtures/`. All targets must be within the project root; no traversal outside. |
| Q1 smoke-test hook ↔ Claude Code runtime | The temporary hook reads stdin from Claude Code and writes JSON to stdout. Stdout is consumed by the runtime; an attacker who could supply arbitrary stdin to the hook could attempt to spoof `stop_hook_active`. Out of scope for v1 (single-user solo tooling). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | PROJECT.md Key Decisions | mitigate | Decision row is appended (not replacing existing rows); reviewer sees diff in commit. |
| T-01-02 | Information disclosure | Q1 smoke test hook | accept | Hook emits a literal marker `Q1-SMOKE-TEST-MARKER` — no PII, no secrets, no project data. Removed after test. |
| T-01-03 | Denial of service | Q1 smoke test hook | mitigate | Hook is a single JSON echo with no loops, no recursion, no external calls. Per Pitfall 3, even non-blocking hooks can loop (#10205) — this hook is removed immediately after one verification turn. |
| T-01-04 | Tampering | Path traversal in fixture write | mitigate | All fixture writes are under `tests/fixtures/` (relative paths only); no `../` segments. Verify with `realpath` containment if uncertain. |
| T-01-05 | Tampering | Accidental write to wiki/Rules.md or wiki/_templates/ | mitigate | This plan never touches those paths. Verified by `files_modified` frontmatter not listing them. (Future plans honor D-16/DIGS-13.) |
| T-01-06 | Tampering | wiki/inbox/_session.md placeholder content drift | accept | Placeholder is a static, zero-entry file; Plan 04 explicitly overwrites it for acceptance run. No contract beyond "file exists with header and zero handle lines". |

ASVS Level 1. Low-risk plan: local file writes only, no network surface beyond a one-time docs fetch and a transient stop hook.
</threat_model>

<verification>
- Q1 row present in PROJECT.md Key Decisions, mechanism is one of two locked values
- Three .gitkeep anchors exist
- Persistent wiki/inbox/_session.md placeholder exists with standard header and zero entries
- Fixture has 7 handle lines covering 5 sub-cases (sub-case 2 by absence)
- Fixture README documents acceptance gates
- No Phase 2/3 territory has been touched (.claude/skills/inbox-update/, .claude/hooks/ absent)
</verification>

<success_criteria>
- Phase 3 has an unambiguous answer to Q1 (PROJECT.md row exists with mechanism + evidence + Locked status)
- Wave 2 plans (02, 03) have anchor directories ready to populate
- ROADMAP Phase 1 Success Criterion 1 is fully satisfied (placeholder `wiki/inbox/_session.md` exists)
- Plan 04 has a fixture to run digest against
- All Phase 1 acceptance criteria are documented in tests/fixtures/README.md ready for Plan 04 to validate
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-curator/01-01-SUMMARY.md` documenting:
- Q1 chosen mechanism + smoke-test outcome
- The fixture's 7-entry / 5-sub-case structure
- Confirmed scaffolding paths (including the persistent wiki/inbox/_session.md placeholder)
- Anything that surprised the agent during the smoke test (will inform Phase 3)
</output>
</content>
</invoke>