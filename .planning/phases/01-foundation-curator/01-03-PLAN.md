---
phase: 01-foundation-curator
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .claude/skills/digest/SKILL.md
  - .claude/skills/digest/.gitkeep
  - .claude/skills/digest/reference/non-fork-fallback.md
autonomous: true
requirements:
  - DIGS-01
  - DIGS-02
  - DIGS-03
  - DIGS-08
  - DIGS-09
  - DIGS-11
  - LIFE-02
  - LIFE-03
tags:
  - claude-code
  - skills
  - digest
  - subagents
  - lifecycle
must_haves:
  truths:
    - "A digest skill exists at .claude/skills/digest/SKILL.md and produces /digest as a slash command"
    - "Skill frontmatter includes `context: fork` and `agent: wiki-curator` (D-13 primary path)"
    - "Skill orchestrates: archive-before-write (LIFE-02, LIFE-03), then plan emission (DIGS-03), then apply, then post-write link audit (DIGS-11)"
    - "Skill body delegates routing/edit/split/link-rewrite logic to wiki-curator (Plan 02) — does not duplicate that protocol"
    - "Auto-rewrite for splits picks per-backlink target by surrounding context (D-05, D-06) — restated to the curator via the skill body's instructions"
    - "Aliased links [[Old|alias]] rewritten as [[New|alias]] (D-07) — restated"
    - "A non-fork fallback path is documented at .claude/skills/digest/reference/non-fork-fallback.md per D-13"
    - "Idempotence (DIGS-12): a digest on an empty inbox is a no-op — handled by curator's Step 1 (no entries → no plan rows → archive empty file → done)"
  artifacts:
    - path: ".claude/skills/digest/SKILL.md"
      provides: "/digest slash command + curator orchestration"
      contains: "context: fork"
      min_lines: 80
    - path: ".claude/skills/digest/reference/non-fork-fallback.md"
      provides: "Non-fork invocation procedure for CLAUDE_CODE_FORK_SUBAGENT=0"
      contains: "wiki-curator"
      min_lines: 20
  key_links:
    - from: ".claude/skills/digest/SKILL.md frontmatter"
      to: ".claude/agents/wiki-curator.md"
      via: "agent: wiki-curator (string reference, resolved at fork)"
      pattern: "agent:\\s*wiki-curator"
    - from: ".claude/skills/digest/SKILL.md body"
      to: "wiki/inbox/_archive/<timestamp>-session.md"
      via: "archive-before-write step (LIFE-02, LIFE-03, D-14)"
      pattern: "wiki/inbox/_archive"
    - from: ".claude/skills/digest/SKILL.md body"
      to: "DIGS-11 link validation"
      via: "post-write audit pass invoked after curator applies the plan"
      pattern: "DIGS-11|link.*audit|unresolved.*\\[\\["
---

<objective>
Build the `/digest` slash command's skill body. The skill's job is ORCHESTRATION: it archives the inbox before any note write (LIFE-02, LIFE-03, D-14), forks into the `wiki-curator` subagent (D-13), and the curator (defined in Plan 02) does the actual routing/template/conflict/split/link-rewrite work. After the curator applies the plan, the skill body invokes a post-write link audit (DIGS-11). A non-fork fallback path is documented for environments without `CLAUDE_CODE_FORK_SUBAGENT=1`.

Purpose: Plan 02 owns the curator's REASONING (what gets routed where, what gets edited vs created, how splits rewrite backlinks). Plan 03 owns the LIFECYCLE around it (archive ordering, fork invocation, post-write audit). Splitting these gives clean wave-2 parallelism with no file overlap.

Output: A `digest/SKILL.md` skill file plus a fallback reference document. The skill must NOT duplicate the curator's routing logic — it points at the curator and trusts it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-curator/01-CONTEXT.md
@.planning/phases/01-foundation-curator/01-01-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@wiki/Rules.md

<interfaces>
<!-- Claude Code skill (SKILL.md) frontmatter for /digest: -->
<!--   name: digest                       — required, becomes /digest -->
<!--   description: <text>                — required, used for model invocation -->
<!--   disable-model-invocation: true     — D-13/PROJECT.md: never auto-trigger; user invokes -->
<!--   context: fork                      — primary path: spawn fresh-context subagent -->
<!--   agent: wiki-curator                — string reference to .claude/agents/wiki-curator.md -->
<!--   allowed-tools: Read, Glob, Grep, Bash — needed for inbox read + tree listing + archive mv -->

<!-- Curator (Plan 02) responsibilities (DO NOT duplicate in this skill): -->
<!--   - Routing (DIGS-04), template (DIGS-05), kebab-case (DIGS-06), summary cap (DIGS-07) -->
<!--   - Same-concept detection (DIGS-09, D-04), splits with backlink rewrite (DIGS-08, D-05, D-06, D-07) -->
<!--   - Hybrid override (D-01, D-02), validation gate before apply (Pattern 5) -->
<!--   - No-modification of Rules.md (DIGS-13, D-16) -->

<!-- This skill's responsibilities (LIFECYCLE around the curator): -->
<!--   - Archive-before-write (LIFE-02, LIFE-03, D-14) -->
<!--   - Fork invocation primary path; named-subagent fallback (D-13) -->
<!--   - Post-write link audit (DIGS-11) -->
<!--   - Idempotence on empty inbox (DIGS-12) — emerges from curator's Step 1 + this skill's archive of an empty file -->

<!-- Archive path convention (D-14, ARCHITECTURE.md): -->
<!--   wiki/inbox/_archive/<ISO-8601-timestamp>-session.md -->
<!--   ISO-8601 format with no colons (e.g., 2026-04-28T1430-session.md) for filesystem safety -->

<!-- bash injection (research/STACK.md): skill body can use !`cmd` to execute and inline output. -->
<!-- This is gated by `disableSkillShellExecution`. Document the gate; provide Read/Glob fallback. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author digest SKILL.md (orchestration only — no curator-logic duplication)</name>
  <files>.claude/skills/digest/SKILL.md</files>
  <read_first>
    - .planning/research/STACK.md §"`.claude/skills/digest-wiki/SKILL.md`" (sketch — note canonical name in CONTEXT.md is `digest`, not `digest-wiki`)
    - .planning/research/ARCHITECTURE.md §"Pattern 4: Subagent with Curated Inputs" + §"Digest skill sketch" (bash injection pattern)
    - .planning/research/PITFALLS.md §"Pitfall 11" (preview-by-default), §"Pitfall 6" (concurrent sessions — out of scope for v1 but referenced)
    - .planning/phases/01-foundation-curator/01-CONTEXT.md §"D-13" (fork primary, named-subagent fallback) and §"D-14" (archive-before-write)
    - wiki/Rules.md §6 (inbox processing workflow — Steps 6-7 of curator's protocol echo this)
  </read_first>
  <behavior>
    The skill MUST:
    - Be invocable as `/digest` (no `-wiki` suffix per CONTEXT.md naming).
    - NOT auto-invoke (`disable-model-invocation: true`) — the user explicitly runs it.
    - PRIMARY path: `context: fork` + `agent: wiki-curator`.
    - ARCHIVE the inbox to `wiki/inbox/_archive/<timestamp>-session.md` BEFORE the curator writes any new notes (LIFE-02, LIFE-03). This is the load-bearing crash-safety property.
    - HANDLE the empty-inbox case (DIGS-12 idempotence): if `wiki/inbox/_session.md` is empty or contains zero `^@ ` handles, archive a copy (still LIFE-02 compliant) and exit with "no-op" message.
    - INVOKE the curator with the inbox payload + wiki tree as inputs.
    - After the curator applies its plan, RUN a post-write link audit (DIGS-11): glob `wiki/**/*.md`, extract every `[[X]]` reference, verify each title resolves to an existing note, surface unresolved links in the digest summary.
    - DOCUMENT the non-fork fallback path inline (D-13) and link to the reference file Task 2 will create.

    The skill MUST NOT:
    - Duplicate the curator's routing/template/split/link-rewrite logic (that's Plan 02).
    - Modify `wiki/Rules.md` or `wiki/_templates/` (DIGS-13, D-16, Rules.md §9).
    - Skip the archive step "for empty inboxes" — D-14 says archive THEN write, with no exception (idempotent on empty).
  </behavior>
  <action>
    Use the Write tool to create `.claude/skills/digest/SKILL.md` with EXACTLY this frontmatter (the body's prose may be reworded but every numbered step and every cited decision ID must remain):

    ```markdown
    ---
    name: digest
    description: Process the rolling session inbox at wiki/inbox/_session.md (or a fixture path) into filed wiki notes per wiki/Rules.md. Archives the inbox before any note write, then forks into the wiki-curator subagent for routing. Run manually as a review checkpoint. Never modifies wiki/Rules.md.
    disable-model-invocation: true
    context: fork
    agent: wiki-curator
    allowed-tools: Read, Glob, Grep, Bash
    argument-hint: "[optional inbox path; defaults to wiki/inbox/_session.md]"
    ---

    # Digest the session inbox

    The user invokes `/digest` after a stretch of work and wants the rolling inbox at `wiki/inbox/_session.md` (or a fixture, e.g. `tests/fixtures/_session-fixture.md` for Phase 1 acceptance) consolidated into properly-filed wiki notes per `wiki/Rules.md`.

    This skill's body is the prompt the wiki-curator subagent receives when forked. The curator (defined at `.claude/agents/wiki-curator.md`) owns the routing, template, conflict-detection, split, and backlink-rewrite logic. This skill body owns the LIFECYCLE around the curator's work: archive-before-write, post-write link audit, and the empty-inbox no-op path.

    ## Resolve the inbox path

    If `$ARGUMENTS` is non-empty, use it as the inbox path. Otherwise default to `wiki/inbox/_session.md`. Common values:
    - `wiki/inbox/_session.md` (live session state, populated by Phase 2's inbox-update skill)
    - `tests/fixtures/_session-fixture.md` (Phase 1 hand-authored fixture for acceptance testing)

    ## Step 1 — Confirm the inbox exists and inspect it

    Read the resolved inbox path. If the file does not exist: print "No inbox found at <path>; nothing to digest." and exit. This is not an error.

    Count the entry handle lines:
    !`grep -cE '^@ (ARCHITECTURE|FUNCTIONS|RESEARCH|SELF|DIAGRAMS)::' "$INBOX_PATH" || echo 0`

    If the count is zero, this digest is a no-op (DIGS-12 idempotence). Still archive the empty file in Step 2 (D-14: archive THEN write, with no exception). After archiving, print "Inbox is empty (0 entries); idempotent no-op." and exit.

    ## Step 2 — Archive the inbox BEFORE any note write (LIFE-02, LIFE-03, D-14)

    Compute a timestamp: ISO-8601 with no colons (filesystem-safe). Example: `2026-04-28T1430-session.md`.

    Create the archive directory if it does not exist:
    !`mkdir -p wiki/inbox/_archive`

    Copy the inbox to the archive:
    !`cp "$INBOX_PATH" "wiki/inbox/_archive/${TIMESTAMP}-session.md"`

    Verify the copy succeeded by re-reading the archive file. If it failed, ABORT — do not proceed to writes. The crash-safety guarantee depends on the archive existing before any new note write.

    Why this ordering: per LIFE-03, a curator crash mid-write would otherwise lose entries. Archive-first ensures every entry the curator was about to file is recoverable from the archive.

    ## Step 3 — Gather inputs for the curator

    The curator needs: (a) the inbox content, (b) the existing wiki tree, (c) wiki/Rules.md (loaded by the wiki-rules skill the curator preloads — but provide it inline as a defense against `disableSkillShellExecution` interactions).

    Inputs:

    - Inbox content:
      !`cat "$INBOX_PATH"`

    - Existing wiki tree (excluding inbox/ and _templates/):
      !`find wiki -type f -name '*.md' -not -path 'wiki/inbox/*' -not -path 'wiki/_templates/*' | sort`

    - Wiki rules (authoritative — re-read fresh per Pitfall 12, NOT a stale skill-body mirror):
      !`cat wiki/Rules.md`

    If `disableSkillShellExecution` is set on this Claude Code instance, the bash injections above produce empty strings. In that case, the curator must Read each input itself using its Read/Glob tools. The curator's prompt covers this fallback.

    ## Step 4 — Curator runs (this is where the fork happens)

    Per the frontmatter `context: fork` + `agent: wiki-curator`, this skill body is delivered to a forked wiki-curator subagent in a fresh context. The curator follows its 8-step protocol (defined in `.claude/agents/wiki-curator.md`), producing:

    1. A markdown plan listing every CREATE / EDIT / SPLIT / OVERRIDE / RULES-PROPOSAL row.
    2. A validation result against Rules.md.
    3. The applied changes (writes / edits to `wiki/<CATEGORY>/<slug>.md`).

    Per D-03: approving the plan as a whole authorizes every row. The curator does NOT prompt for per-row confirmation.

    Restated reminders to the curator (these are also in its system prompt — listed here for redundancy because skill bodies can be fork-injected and prompt context drift is real):

    - **DIGS-08 + D-05 + D-06 + D-07 (splits + backlinks):** When splitting a note, grep `wiki/**/*.md` for inbound `[[OldTitle]]` and `[[OldTitle|alias]]` references. For EACH inbound link, choose the most relevant split target based on the surrounding sentence and the section heading the link sits under. Rewrite in place. Aliased links preserve the alias: `[[OldTitle|alias]]` becomes `[[NewTitle|alias]]`. Report as ONE summary line in the plan.
    - **DIGS-09 + D-04 (existing-note conflict):** Same-concept detection by filename + title + tag overlap. NEVER use embeddings (anti-feature A10). 2+ matching signals → EDIT existing note (bump Last Updated). 0–1 → CREATE new.
    - **DIGS-13 + D-16:** Curator NEVER writes to `wiki/Rules.md`. Rule-change suggestions surface as RULES-PROPOSAL rows for the user to apply manually.
    - **Path safety:** Every write target must be under `wiki/`. Denylist: `wiki/Rules.md`, `wiki/_templates/**`. Reject any plan row violating this.

    ## Step 5 — Post-write link audit (DIGS-11, D-08)

    After the curator finishes applying its plan, run a link-validation pass:

    !`grep -roE '\[\[[^\]]+\]\]' wiki --include='*.md' | grep -v 'wiki/inbox/_archive' | sort -u`

    For each unique `[[X]]` reference (or `[[X|alias]]` — strip the `|alias` part), verify a note exists with H1 matching X. Cheap heuristic: glob `wiki/**/*.md` and check whether any file's first non-frontmatter heading is `# X` (case-insensitive). Surface unresolved references in the digest summary as a section "Unresolved wiki-links".

    Per D-08: this audit covers BOTH pre-existing dangling links AND any links the curator failed to rewrite during a split. It is the safety net.

    ## Step 6 — Emit the digest summary

    Final output to the user:

    ```
    ## Digest Summary

    **Inbox archived to:** wiki/inbox/_archive/<TIMESTAMP>-session.md
    **Entries processed:** <N>
    **Notes created:** <list>
    **Notes edited:** <list>
    **Splits performed:** <list with backlink rewrite counts>
    **Overrides applied:** <list of FUNCTIONS→ARCHITECTURE etc. with reasons>
    **Rule-change proposals (NOT applied — for user to consider):** <list>
    **Unresolved wiki-links:** <list, or "none">
    ```

    ## Non-fork fallback (D-13)

    If the user's environment has `CLAUDE_CODE_FORK_SUBAGENT=0` (or unset, depending on Claude Code version), `context: fork` falls back to inline execution and the fork isolation is lost. The procedure for invoking wiki-curator without forking is documented at `.claude/skills/digest/reference/non-fork-fallback.md`. The curator's protocol is identical; only the invocation path changes.

    ## Things this skill does NOT do

    - Does NOT route, template, or detect duplicates itself — that's the curator's job.
    - Does NOT modify `wiki/Rules.md` (DIGS-13).
    - Does NOT auto-commit anything (anti-feature A11).
    - Does NOT write to `wiki/inbox/_session.md` after digest — Phase 2's inbox-update skill owns that. This skill leaves the live inbox path untouched after archiving (the curator may truncate it as part of LIFE-02 lifecycle reset, but content writing belongs to Phase 2).
    ```

    Notes for the executor:
    - The bash injections (`!`...``) require `disableSkillShellExecution: false`. The body explicitly handles the disabled case.
    - `$ARGUMENTS` is the standard Claude Code skill substitution for argument-hint.
    - The `agent: wiki-curator` reference resolves to `.claude/agents/wiki-curator.md` at fork time.
    - Replace `${TIMESTAMP}` placeholders with a real bash variable expansion or compute via `date +%Y-%m-%dT%H%M`. The spec is the format, not the literal string.
    - The `.gitkeep` from Plan 01 may be left alongside SKILL.md or removed — both work.
  </action>
  <acceptance_criteria>
    - File `.claude/skills/digest/SKILL.md` exists with valid YAML frontmatter
    - Frontmatter contains `name: digest` (NOT `digest-wiki`)
    - Frontmatter contains `disable-model-invocation: true`
    - Frontmatter contains `context: fork`
    - Frontmatter contains `agent: wiki-curator`
    - Frontmatter `allowed-tools` lists at minimum `Read`, `Glob`, `Grep`, `Bash` (Bash needed for archive `mkdir`/`cp`)
    - Body contains the literal substring `wiki/inbox/_archive` (archive path convention)
    - Body contains `LIFE-02` AND `LIFE-03` (archive-before-write)
    - Body contains `DIGS-11` (post-write link audit)
    - Body contains `DIGS-12` (idempotence on empty inbox)
    - Body contains `DIGS-13` (no-Rules-modification reminder)
    - Body contains `D-04`, `D-05`, `D-06`, `D-07`, `D-13`, `D-14` (decision traceability)
    - Body explicitly mentions `disableSkillShellExecution` (interaction documented)
    - Body explicitly mentions `CLAUDE_CODE_FORK_SUBAGENT` (fallback path)
    - Body does NOT contain the literal regex `^[a-z0-9][a-z0-9-]*\.md$` for filenames AS A NEW RULE — this rule belongs to the curator (Plan 02), not the digest skill. The skill may MENTION DIGS-06 but must not duplicate the regex enforcement.
    - File is at least 80 lines long
  </acceptance_criteria>
  <verify>
    <automated>test -f .claude/skills/digest/SKILL.md && grep -q "^name: digest$" .claude/skills/digest/SKILL.md && grep -q "^disable-model-invocation: true$" .claude/skills/digest/SKILL.md && grep -q "^context: fork$" .claude/skills/digest/SKILL.md && grep -q "^agent: wiki-curator$" .claude/skills/digest/SKILL.md && grep -qE "allowed-tools:.*Read.*Glob.*Grep.*Bash" .claude/skills/digest/SKILL.md && grep -q "wiki/inbox/_archive" .claude/skills/digest/SKILL.md && grep -q "LIFE-02" .claude/skills/digest/SKILL.md && grep -q "LIFE-03" .claude/skills/digest/SKILL.md && grep -q "DIGS-11" .claude/skills/digest/SKILL.md && grep -q "DIGS-12" .claude/skills/digest/SKILL.md && grep -q "DIGS-13" .claude/skills/digest/SKILL.md && grep -q "D-14" .claude/skills/digest/SKILL.md && grep -q "disableSkillShellExecution" .claude/skills/digest/SKILL.md && grep -q "CLAUDE_CODE_FORK_SUBAGENT" .claude/skills/digest/SKILL.md && [ "$(wc -l < .claude/skills/digest/SKILL.md)" -ge 80 ]</automated>
  </verify>
  <done>digest/SKILL.md exists with correct frontmatter; orchestrates archive-before-write + fork-into-curator + post-write audit; explicitly does not duplicate curator routing logic; all 9 of this plan's requirements have explicit support.</done>
</task>

<task type="auto">
  <name>Task 2: Author non-fork fallback reference (D-13)</name>
  <files>.claude/skills/digest/reference/non-fork-fallback.md</files>
  <read_first>
    - .planning/research/STACK.md §"Version Compatibility" (notes CLAUDE_CODE_FORK_SUBAGENT experimental status)
    - .planning/research/ARCHITECTURE.md §"Failure Modes" (T8 fallback discussion)
    - .planning/phases/01-foundation-curator/01-CONTEXT.md §"D-13" (the canonical agent name `wiki-curator` per CONTEXT.md is the only thing this fallback doc cross-references; do NOT read .claude/agents/wiki-curator.md from Plan 02 — preserves Plan 03's wave-2 parallelism with Plan 02 per B4)
  </read_first>
  <action>
    Create `.claude/skills/digest/reference/non-fork-fallback.md`. This is a supporting-files reference document the digest skill can point at; it documents how to invoke the agent named `wiki-curator` (per CONTEXT.md D-13) without `context: fork`.

    Per B4: this task does NOT cross-reference the curator file's content (its protocol step count, internal headings, etc.) — it only references the agent NAME. This preserves Plan 03's wave-2 parallelism with Plan 02 (no read dependency on Plan 02's output).

    Use the Write tool with this content:

    ```markdown
    # Digest non-fork fallback

    **When this applies:** the consumer's Claude Code does not have `CLAUDE_CODE_FORK_SUBAGENT=1` set, OR the local Claude Code version does not support `context: fork` (experimental as of v2.1.117 per research/STACK.md).

    **Symptom:** when the user runs `/digest`, the skill body executes in the parent session instead of forking into a fresh context. This pollutes the parent context with the inbox + wiki tree + Rules.md but otherwise still works.

    ## Procedure

    1. The digest skill body's primary path is `context: fork` + `agent: wiki-curator` (the agent named `wiki-curator` per CONTEXT.md D-13). If forking is unavailable, the skill body still runs — the current Claude in the parent session reads the body and executes it.

    2. To preserve the curator's behavior without true context isolation, the parent Claude must:
       - Treat the wiki-curator system prompt at `.claude/agents/wiki-curator.md` as authoritative for this run. Read that file fresh at fallback time.
       - Treat the wiki-rules skill body at `.claude/skills/wiki-rules/SKILL.md` as authoritative. Read that file fresh.
       - Follow whatever protocol the curator file defines (its sections are authoritative — this fallback doc does not duplicate them).
       - Continue using the digest skill body's lifecycle steps (archive-before-write, post-write audit).

    3. The only LOST property in the fallback path is fresh-context isolation. The curator's protocol is correctness-equivalent across both paths.

    ## Why fork is preferred

    - **Context budget:** forking gives the curator a fresh window. In the parent session, the curator's reading of wiki/Rules.md + inbox + wiki tree adds 5–20 KB to the parent's context for the rest of the session.
    - **Determinism:** forked subagent has nothing in context except the skill body, the agent system prompt, and preloaded skills. No accidental influence from the parent's prior turns.
    - **Cleanup:** when the fork ends, only the digest summary returns. In the fallback path, the parent session is left with the curator's intermediate work in its context.

    None of these affect correctness. They affect ergonomics and cost.

    ## Detection (optional)

    The digest skill body can detect the fallback path by checking the environment variable `CLAUDE_CODE_FORK_SUBAGENT`:

    ```bash
    if [ "${CLAUDE_CODE_FORK_SUBAGENT:-0}" != "1" ]; then
      echo "WARNING: forking unavailable; using fallback path. See .claude/skills/digest/reference/non-fork-fallback.md"
    fi
    ```

    The warning is informational only — the digest still proceeds.

    ## Reference

    - D-13 in `.planning/phases/01-foundation-curator/01-CONTEXT.md` (canonical agent name + fallback semantics)
    - "Version Compatibility" table in `.planning/research/STACK.md`
    - "Failure Modes T8" in `.planning/research/ARCHITECTURE.md`
    ```
  </action>
  <acceptance_criteria>
    - File `.claude/skills/digest/reference/non-fork-fallback.md` exists
    - File contains literal `wiki-curator` (the agent NAME — only cross-ref to Plan 02 needed per B4)
    - File contains literal `CLAUDE_CODE_FORK_SUBAGENT`
    - File contains literal `D-13`
    - File does NOT cross-reference the curator file's internal protocol step count (preserves wave-2 parallelism)
    - File is at least 20 lines long
  </acceptance_criteria>
  <verify>
    <automated>test -f .claude/skills/digest/reference/non-fork-fallback.md && grep -q "wiki-curator" .claude/skills/digest/reference/non-fork-fallback.md && grep -q "CLAUDE_CODE_FORK_SUBAGENT" .claude/skills/digest/reference/non-fork-fallback.md && grep -q "D-13" .claude/skills/digest/reference/non-fork-fallback.md && [ "$(wc -l < .claude/skills/digest/reference/non-fork-fallback.md)" -ge 20 ]</automated>
  </verify>
  <done>Fallback reference file exists, references the wiki-curator agent name and the env var; does NOT cross-reference Plan 02's protocol step count (B4 — preserves wave-2 parallelism).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /digest skill ↔ filesystem | Skill executes Bash injections (mkdir, cp, find, grep). Bash runs in the parent session before fork; an attacker who could substitute the inbox path argument could attempt path traversal. |
| skill body ↔ forked curator | The skill body becomes the curator's prompt. A poisoned skill body is a poisoned curator. Mitigated by: skill is checked into git; install flow (Phase 4) merges rather than overwrites. |
| Archive directory ↔ pre-existing files | `cp` to `wiki/inbox/_archive/<TIMESTAMP>-session.md` could collide if two digests fire within the same minute. Out of scope for v1 (single-developer; HARD-01 Phase 5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Path traversal via $ARGUMENTS | mitigate | Skill body's Step 1 reads the resolved path; if it does not exist, exits cleanly. The cp in Step 2 uses the same resolved path; cp does not interpret traversal segments specially, but the destination is hard-coded under `wiki/inbox/_archive/`. Verify in Plan 04 that a malicious path argument cannot escape. Document: argument is trusted-source (user-supplied at slash-command time). |
| T-03-02 | Information disclosure | Bash injection prints inbox + Rules.md content | accept | Both files are local user-owned markdown. Skill body runs in user's own session. No PII or secrets in scope. |
| T-03-03 | Denial of service | Curator hangs on huge inbox | accept | v1 scope: PROJECT.md/CONTEXT.md establish single-developer manual digest. Pitfall 13 (chunked digest mode) is HARD-02, deferred to v2. |
| T-03-04 | Tampering | Archive overwrite if two digests within same minute | accept | Single-developer scope (HARD-01). Filename uses minute-resolution timestamp; if collision occurs, second digest overwrites the first archive. Phase 5 hardens with sub-second resolution or per-session lockfile. |
| T-03-05 | Tampering | Post-write audit grep includes wiki/inbox/_archive | mitigate | Step 5 grep explicitly excludes `wiki/inbox/_archive` paths so old archived `[[X]]` references do not pollute the unresolved-link report. |
| T-03-06 | Tampering | Curator escapes wiki/ via auto-rewrite | mitigate | Curator (Plan 02) restricts grep+rewrite scope to `wiki/**/*.md`. Plan 04 verifies no `.planning/`, `src/`, or `.claude/` files mutate during a digest run. |

ASVS Level 1. Local-only file ops; no network; one bash subshell per injection.
</threat_model>

<verification>
- digest/SKILL.md exists with correct frontmatter (name=digest, disable-model-invocation=true, context=fork, agent=wiki-curator)
- Skill orchestrates: archive (LIFE-02, LIFE-03, D-14) → curator-fork (D-13) → post-write audit (DIGS-11)
- Empty-inbox idempotence (DIGS-12) handled
- disableSkillShellExecution interaction documented
- Non-fork fallback (D-13) documented in reference file with explicit pointer from SKILL.md
- All 9 requirements (DIGS-01, 02, 03, 08, 09, 10, 11, LIFE-02, LIFE-03) present in the skill body's text
</verification>

<success_criteria>
- A user running `/digest` against `tests/fixtures/_session-fixture.md` reaches the curator with the inbox payload + wiki tree + Rules.md, having archived first
- The curator (Plan 02) does the routing; this skill does not duplicate that work
- After the curator applies, the post-write audit catches any `[[X]]` link that doesn't resolve
- An empty inbox produces an idempotent no-op (still archives, prints "no-op", exits)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-curator/01-03-SUMMARY.md` documenting:
- Final frontmatter (in case of any deviations from the plan due to local Claude Code constraints)
- Whether bash injection in the skill body was preserved or replaced with a Read/Glob fallback for the disableSkillShellExecution case
- Any decisions about the timestamp format that diverged from `YYYY-MM-DDTHHMM`
- Notes on the post-write audit grep — exact pattern used, performance characteristics on the Traxalytics fixture wiki
</output>
