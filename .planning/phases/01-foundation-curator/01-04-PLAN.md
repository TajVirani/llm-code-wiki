---
phase: 01-foundation-curator
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - wiki/inbox/_session.md
  - .planning/phases/01-foundation-curator/01-04-acceptance-report.md
  - .claude/agents/wiki-curator.md
  - .claude/skills/digest/SKILL.md
autonomous: false
requirements:
  - DIGS-01
  - DIGS-02
  - DIGS-03
  - DIGS-04
  - DIGS-05
  - DIGS-06
  - DIGS-07
  - DIGS-08
  - DIGS-09
  - DIGS-10
  - DIGS-11
  - DIGS-12
  - DIGS-13
  - LIFE-02
  - LIFE-03
tags:
  - acceptance
  - testing
  - integration
must_haves:
  truths:
    - "The fixture inbox copied to wiki/inbox/_session.md, /digest run, and result captured against acceptance criteria"
    - "≥80% of fixture entries land in correct category folder (ROADMAP success criterion)"
    - "0 Rules.md violations on filed notes (filename regex, ≤25-word summary, all template fields, [[Title]] wiki-links)"
    - "Sub-case 2 produces zero filed notes for any slug absent from the fixture (CAPT-04 release blocker validated in Phase 1)"
    - "Sub-case 3 produces an OVERRIDE plan line for the auth-boundary-policy entry (D-01 Hybrid override)"
    - "Sub-case 4 produces an EDIT plan row for wiki/FUNCTIONS/vorp-batch-processing.md (DIGS-09)"
    - "Sub-case 5 produces a SPLIT plan row with N>0 backlink rewrites — per-link target chosen by surrounding context (D-05, D-06)"
    - "Idempotence (DIGS-12): a SECOND digest run on the post-archive empty inbox produces zero changes"
    - "Archive-before-write verified: wiki/inbox/_archive/<timestamp>-session.md exists with the pre-digest fixture content"
    - "Curator did NOT modify wiki/Rules.md (DIGS-13, D-16)"
    - "Curator did NOT write outside wiki/ (no .planning/, .claude/, src/, tests/ mutations)"
    - "Aliased link [[Old|alias]] (introduced as part of test setup) was rewritten to [[New|alias]] (D-07)"
  artifacts:
    - path: ".planning/phases/01-foundation-curator/01-04-acceptance-report.md"
      provides: "Per-sub-case acceptance results, with pass/fail and evidence"
      min_lines: 60
    - path: "wiki/inbox/_archive/<timestamp>-session.md"
      provides: "Archive of the fixture run (LIFE-02, LIFE-03 evidence)"
  key_links:
    - from: "tests/fixtures/_session-fixture.md (Plan 01 output)"
      to: "wiki/inbox/_session.md"
      via: "copy at start of acceptance test"
      pattern: "cp.*tests/fixtures.*wiki/inbox"
    - from: "/digest invocation"
      to: "wiki-curator subagent (Plan 02)"
      via: "context: fork in digest/SKILL.md (Plan 03)"
      pattern: "agent:\\s*wiki-curator"
---

<objective>
Run the Phase 1 acceptance gate. Copy the fixture inbox to the live path, invoke `/digest`, and verify every acceptance criterion documented in `tests/fixtures/README.md` against the resulting plan + filed notes + archive. If any criterion fails, iterate the curator system prompt (Plan 02 output) and the digest skill body (Plan 03 output) until all pass. This plan exists specifically to make the Phase 1 success criteria mechanically verifiable rather than asserting "the curator works" by inspection.

Purpose: Plans 02 and 03 produce the components. Plan 04 produces EVIDENCE that they work together against the fixture. Without this, "Phase 1 success" is unfalsifiable.

Output: An acceptance report documenting per-sub-case results, an archived inbox proving LIFE-02/03, and (if iteration was needed) the final iterated curator + digest skill files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-curator/01-CONTEXT.md
@.planning/phases/01-foundation-curator/01-01-SUMMARY.md
@.planning/phases/01-foundation-curator/01-02-SUMMARY.md
@.planning/phases/01-foundation-curator/01-03-SUMMARY.md
@tests/fixtures/_session-fixture.md
@tests/fixtures/README.md
@.claude/agents/wiki-curator.md
@.claude/skills/digest/SKILL.md
@.claude/skills/wiki-rules/SKILL.md
@wiki/Rules.md
@wiki/_templates/note.md
@wiki/FUNCTIONS/vorp-batch-processing.md

<interfaces>
<!-- Sub-case acceptance criteria (from tests/fixtures/README.md, restated): -->
<!-- 1. Happy path: ≥80% of 5 entries route correctly (≥4 of 5) -->
<!-- 2. 1+1-then-deleted: zero filed notes for any slug absent from fixture (CAPT-04) -->
<!-- 3. Ambiguous: plan contains an OVERRIDE line for `auth-boundary-policy` -->
<!-- 4. Existing-note conflict: plan contains EDIT row for `wiki/FUNCTIONS/vorp-batch-processing.md` -->
<!-- 5. Split-implication: plan contains SPLIT row with N>0 backlink rewrites for vorp-batch-processing -->
<!-- Idempotence (DIGS-12): second /digest on empty inbox = no-op, zero new files in wiki/ -->

<!-- The aliased-link test (D-07): Task 1 below introduces ONE [[VORP Batch Processing|alias]] -->
<!-- somewhere in the wiki BEFORE the digest, then verifies the SPLIT rewrites preserve the alias. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up the test environment (copy fixture to live path, plant aliased-link probe)</name>
  <files>wiki/inbox/_session.md, wiki/RESEARCH/vorp-related-research.md</files>
  <read_first>
    - tests/fixtures/_session-fixture.md (Plan 01 output)
    - tests/fixtures/README.md (acceptance criteria)
    - wiki/FUNCTIONS/vorp-batch-processing.md (the existing note that sub-cases 4+5 target — confirms its H1/title for the alias probe)
    - wiki/RESEARCH/ (directory listing — pick a real existing RESEARCH note OR create a minimal one for the alias probe)
  </read_first>
  <action>
    Two setup steps before the digest run.

    **Step A: Copy fixture to live inbox path.**

    The fixture lives at `tests/fixtures/_session-fixture.md` (Plan 01). The digest skill expects to read `wiki/inbox/_session.md` by default. Copy:

    ```bash
    mkdir -p wiki/inbox
    cp tests/fixtures/_session-fixture.md wiki/inbox/_session.md
    ```

    Verify with:
    ```bash
    diff tests/fixtures/_session-fixture.md wiki/inbox/_session.md
    # should be empty / no diff
    ```

    **Step B: Plant the aliased-link probe (D-07 verification).**

    Per B3 (revision): commit to a single unconditional approach to keep `files_modified` accurate and the verify deterministic. ALWAYS create `wiki/RESEARCH/vorp-related-research.md` as a fresh Rules.md-conformant note — do NOT branch on whether some other RESEARCH note happens to reference VORP batch processing.

    Use the Write tool to create `wiki/RESEARCH/vorp-related-research.md` with this content (Rules.md-conformant note schema):

    ```markdown

    **Summary**: Pointer note linking VORP-related research and the batch calculator that consumes it.
    **Tags**: #research #vorp
    **Created**: 2026-04-28T00:00:00+00:00
    **Last Updated**: 2026-04-28T00:00:00+00:00

    ---

    ## Content

    Plant for D-07 alias-rewrite test. The link below uses the aliased-display form `[[Title|alias]]` and must remain aliased after the SPLIT in the upcoming digest run.

    The batch calculator referenced here is documented at [[VORP Batch Processing|the batch calculator]] — adjacent to the per-job concurrency model.

    ## Related Notes

    - [[VORP Batch Processing|the batch calculator]]
    ```

    The aliased title `VORP Batch Processing` matches the canonical title the curator's same-concept detection uses for `wiki/FUNCTIONS/vorp-batch-processing.md` (derived from filename slug since the existing note has no H1; `**Summary**:` is the first line). This file plants exactly ONE aliased reference for the D-07 verification step in Task 3.
  </action>
  <acceptance_criteria>
    - File `wiki/inbox/_session.md` exists and is byte-identical to `tests/fixtures/_session-fixture.md` (verify with `diff`)
    - There exists exactly ONE occurrence of the substring `[[VORP Batch Processing|` somewhere in `wiki/RESEARCH/**/*.md` (the planted alias probe)
    - The probe file conforms to wiki/_templates/note.md schema (has Summary, Tags, Created, Last Updated, ## Content, ## Related Notes — verified by grep)
    - No file outside wiki/ was modified by this task
  </acceptance_criteria>
  <verify>
    <automated>diff -q tests/fixtures/_session-fixture.md wiki/inbox/_session.md && test -f wiki/RESEARCH/vorp-related-research.md && [ "$(grep -cE '\[\[VORP Batch Processing\|' wiki/RESEARCH/vorp-related-research.md)" -eq 1 ]</automated>
  </verify>
  <done>Fixture copied to live path; aliased-link probe planted at wiki/RESEARCH/vorp-related-research.md (unconditional per B3); both files conform to Rules.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run /digest, capture the plan output, capture the applied changes</name>
  <files>.planning/phases/01-foundation-curator/01-04-acceptance-report.md, wiki/inbox/_archive/, wiki/{ARCHITECTURE,FUNCTIONS,RESEARCH,SELF,DIAGRAMS}/</files>
  <read_first>
    - .claude/skills/digest/SKILL.md (Plan 03 output — the orchestration this run exercises)
    - .claude/agents/wiki-curator.md (Plan 02 output — the routing intelligence)
    - tests/fixtures/README.md (criteria checklist)
  </read_first>
  <what-built>
    Plans 01–03 produced: the fixture, the curator subagent, the wiki-rules skill, the digest skill, the non-fork fallback reference, and the planted aliased-link probe. This task runs the integration test and records the result.
  </what-built>
  <action>
    Run `/digest` against the fixture (now at `wiki/inbox/_session.md` per Task 1). Two paths depending on environment:

    **Primary path (CLAUDE_CODE_FORK_SUBAGENT=1 supported):**
    Invoke the slash command `/digest`. Claude Code forks into the wiki-curator subagent. Capture:
    1. The plan markdown the curator emits BEFORE applying.
    2. The list of files written / edited.
    3. The post-write audit summary (unresolved wiki-links).

    **Fallback path (CLAUDE_CODE_FORK_SUBAGENT not set or 0):**
    Per `.claude/skills/digest/reference/non-fork-fallback.md`, invoke `/digest` and execute its body in the parent session, treating the curator's system prompt as authoritative.

    EITHER path: produce evidence at three points:
    - **Pre-write evidence:** the plan output captured to text (paste into the acceptance report file).
    - **Archive evidence:** verify `wiki/inbox/_archive/<TIMESTAMP>-session.md` exists and contains the fixture's content (LIFE-02, LIFE-03).
    - **Post-write evidence:** list of files created/edited in `wiki/<CATEGORY>/`, plus the audit's unresolved-links list.

    Write the captured output to `.planning/phases/01-foundation-curator/01-04-acceptance-report.md` using this template:

    ```markdown
    # Phase 1 Acceptance Report

    **Run date:** <ISO date>
    **Path used:** primary (fork) | fallback (named subagent)
    **Fixture:** wiki/inbox/_session.md (copied from tests/fixtures/_session-fixture.md)

    ## Captured plan output (curator's pre-write plan)

    ```
    <paste the markdown plan table the curator emitted>
    ```

    ## Captured file changes

    | Action | Path |
    |--------|------|
    | CREATE | wiki/<CATEGORY>/<slug>.md |
    | EDIT   | wiki/<CATEGORY>/<slug>.md |
    | SPLIT  | <old> → <new1>, <new2> |

    ## Captured archive

    `wiki/inbox/_archive/<TIMESTAMP>-session.md`
    Verified: same byte content as the pre-digest fixture? <yes/no>

    ## Captured audit (unresolved wiki-links)

    <list, or "none">

    ## Per-sub-case results

    | # | Sub-case | Expected | Observed | Pass |
    |---|----------|----------|----------|------|
    | 1 | Happy path: ≥80% routed correctly (≥4 of 5) | (4 or 5)/5 | (?) | (?) |
    | 2 | 1+1-then-deleted: 0 notes for absent slugs | 0 | (?) | (?) |
    | 3 | Ambiguous: OVERRIDE line for auth-boundary-policy | present in plan | (?) | (?) |
    | 4 | Existing conflict: EDIT row for vorp-batch-processing.md | present in plan | (?) | (?) |
    | 5 | Split-implication: SPLIT row + N>0 backlink rewrites | present | (?) | (?) |
    | — | Aliased link [[VORP Batch Processing\|alias]] preserved as [[NewSplitTitle\|alias]] (D-07) | preserved | (?) | (?) |
    | — | Idempotence (DIGS-12): 2nd /digest on empty inbox = 0 file changes | 0 | (?) | (?) |
    | — | Archive-before-write (LIFE-02, LIFE-03) | archive exists pre-write | (?) | (?) |
    | — | Curator did NOT modify wiki/Rules.md (DIGS-13) | unmodified | (?) | (?) |
    | — | Curator did NOT write outside wiki/ | no mutations outside wiki/ | (?) | (?) |
    | — | 0 Rules.md violations on filed notes | 0 | (?) | (?) |

    ## Iteration log (if any criteria failed)

    <free-form notes on which curator-prompt or digest-skill changes were made and why>
    ```

    Then: **PAUSE FOR USER**. Present the acceptance report and ask the user to confirm the run executed and the captured outputs are accurate. The user is the source of truth for "did /digest actually fire" and "did the user observe the plan output before files were written".
  </action>
  <how-to-verify>
    1. Open `.planning/phases/01-foundation-curator/01-04-acceptance-report.md`. Verify all sections are filled with concrete observations (no `(?)` placeholders remaining).
    2. Verify `wiki/inbox/_archive/` contains a timestamped session file.
    3. Confirm with the user that the plan output appeared in the conversation BEFORE the writes (DIGS-03, Pattern 5).
    4. Confirm that `wiki/Rules.md` has not been modified (compare against git HEAD).
    5. Confirm no `.planning/`, `.claude/`, or `src/` files were modified beyond this task's expected outputs.
  </how-to-verify>
  <resume-signal>Type "approved — all acceptance criteria pass" OR list which criteria failed so the next task can iterate</resume-signal>
  <acceptance_criteria>
    - File `.planning/phases/01-foundation-curator/01-04-acceptance-report.md` exists
    - Acceptance report's per-sub-case table contains zero `(?)` placeholder entries (every cell filled with concrete observations)
    - At least one file matching `wiki/inbox/_archive/*-session.md` exists
    - The git diff for `wiki/Rules.md` is empty (no modification — DIGS-13 verified)
    - The user has confirmed "approved" or has identified specific failing criteria for Task 3 to address
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/01-foundation-curator/01-04-acceptance-report.md && ! grep -q '(?)' .planning/phases/01-foundation-curator/01-04-acceptance-report.md && ls wiki/inbox/_archive/*-session.md >/dev/null 2>&1 && [ -z "$(git diff -- wiki/Rules.md)" ] && ! grep -iE "SKIP|UNROUTABLE|cannot route|cannot classify" .planning/phases/01-foundation-curator/01-04-acceptance-report.md</automated>
  </verify>
  <done>Digest ran, archive exists, acceptance report fully populated with observations, no SKIP/UNROUTABLE outputs (D-02 negative test passes), user has signed off OR identified specific failures for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Iterate curator + digest skill if any acceptance criteria failed; re-run; verify idempotence</name>
  <files>.claude/agents/wiki-curator.md, .claude/skills/digest/SKILL.md, .planning/phases/01-foundation-curator/01-04-acceptance-report.md</files>
  <read_first>
    - .planning/phases/01-foundation-curator/01-04-acceptance-report.md (Task 2's output — the failures to address)
    - .claude/agents/wiki-curator.md (current state)
    - .claude/skills/digest/SKILL.md (current state)
    - tests/fixtures/README.md (criteria)
    - .planning/research/PITFALLS.md (relevant pitfalls if a specific class of failure shows up)
  </read_first>
  <action>
    Read the acceptance report from Task 2. Branch on outcome:

    **Branch A — All criteria passed in Task 2:**

    No iteration needed. Verify idempotence (DIGS-12):
    1. The fixture run consumed `wiki/inbox/_session.md`. After the digest, that file should be empty (or contain only the standard inbox header with zero entries) — the curator's protocol or the skill body's lifecycle handled the reset. If not empty, truncate it to the standard header now (this is part of LIFE-02 lifecycle).
    2. Run `/digest` a second time.
    3. Verify: zero new files in `wiki/<CATEGORY>/`, zero edits to existing files, the second-run output reports "Inbox is empty (0 entries); idempotent no-op."
    4. Append an "Idempotence run" section to the acceptance report documenting: file count before vs after, edit count, the no-op message.

    **Branch B — One or more criteria failed in Task 2:**

    For each failed criterion, identify whether the fix belongs in the curator system prompt (Plan 02) or the digest skill body (Plan 03):

    | Failing criterion | Likely owner | Likely fix |
    |--------------------|--------------|------------|
    | Routing accuracy <80% | wiki-curator | Sharpen routing prompt with concrete examples; add a worked example for the failed category |
    | Sub-case 2: notes filed for absent slug | wiki-curator (Step 1 read inputs) | Strengthen "only file what's in the inbox" framing in Step 1 |
    | Sub-case 3: curator produced SKIP/UNROUTABLE instead of OVERRIDE (D-02 negative) | wiki-curator (Step 2) | Add D-02 negative example to Step 2 of curator prompt; the override is mandatory, no skip path exists |
    | Sub-case 3: no OVERRIDE line | wiki-curator (Step 2) | Strengthen Hybrid override section; add the auth-boundary-policy example verbatim |
    | Sub-case 4: CREATE instead of EDIT | wiki-curator (Step 3) | Tighten same-concept detection; verify filename match weighting |
    | Sub-case 5: no SPLIT or no backlink rewrites | wiki-curator (Step 4) | Strengthen split detection and per-link target picking; add D-06 worked example |
    | D-07 alias not preserved | wiki-curator (Step 4 last sub-bullet) | Restate alias preservation rule with `[[Old\|alias]] → [[New\|alias]]` |
    | Archive missing or post-write | digest skill | Move archive step earlier in the skill body; verify it ran before any curator writes |
    | Files written outside wiki/ | wiki-curator (path safety) AND digest skill | Tighten path-validation in curator Step 6; add post-fork sandbox check |
    | Rules.md modified | wiki-curator (denylist) | Strengthen denylist; add Step 6 explicit reject for any plan row targeting Rules.md |
    | Rules.md violations on filed notes (filename, summary length, template fields) | wiki-curator (Step 6 lint) | Tighten Step 6 validation gate |

    Apply the fix(es) using the Edit tool on the relevant file. Keep edits MINIMAL and SCOPED to the failed criterion. Do NOT rewrite the whole prompt.

    Then re-run `/digest`. To get a clean run (per W8 — `git checkout` only reverts tracked files; untracked curator writes from a failed run pollute iteration re-runs unless explicitly removed):
    1. Revert tracked wiki/ files AND remove untracked curator writes, preserving the planted aliased-link probe:
       ```bash
       git checkout -- wiki/
       git clean -fd wiki/ -e 'wiki/RESEARCH/vorp-related-research.md'
       cp tests/fixtures/_session-fixture.md wiki/inbox/_session.md
       ```
       The `-e` exclude preserves the D-07 alias probe planted in Task 1; everything else untracked under `wiki/` is removed so the iteration starts from a clean state.
    2. Re-run `/digest`.
    3. Re-fill the acceptance report's per-sub-case table with the new observations. Append a new "Iteration N" section to the report.
    4. Repeat at most 3 iterations. If criteria still fail after 3 iterations, ESCALATE: pause and report which criteria are intractable. The escalation surface is a checkpoint asking the user whether to extend the iteration budget or close Phase 1 with documented gaps.

    After all criteria pass (or after escalation): perform the idempotence run from Branch A. Per W10: the idempotence run is MANDATORY even on the escalation path — an escalation that skips idempotence testing is incomplete. The acceptance report must contain an "Idempotence run" section reporting either a clean no-op (preferred) or, if escalation was taken before all functional criteria passed, an explicit note that idempotence was tested against the partial state and what was observed.

    **Update the acceptance report's Iteration log** with each round of changes (file modified, criterion addressed, outcome).
  </action>
  <acceptance_criteria>
    - The acceptance report's per-sub-case table now shows `Pass: yes` (or equivalent) for every row OR documents an escalation in its Iteration log
    - The acceptance report contains an "Idempotence run" section documenting the second `/digest` produced zero file changes
    - If iteration was needed: the Iteration log lists each round (criterion → file edited → outcome)
    - `git diff` for `wiki/Rules.md` is still empty (no modification — DIGS-13 holds across iterations)
    - At least one timestamped archive file exists in `wiki/inbox/_archive/`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Idempotence run" .planning/phases/01-foundation-curator/01-04-acceptance-report.md && grep -A 8 "Idempotence run" .planning/phases/01-foundation-curator/01-04-acceptance-report.md | grep -qiE "no-op|0 (changes|file changes|new files)|zero (changes|file changes|new files)|partial state" && [ -z "$(git diff -- wiki/Rules.md)" ] && ls wiki/inbox/_archive/*-session.md >/dev/null 2>&1 && ! grep -qE '^\| [0-9—]+ \|.*\| no \|' .planning/phases/01-foundation-curator/01-04-acceptance-report.md</automated>
  </verify>
  <done>All acceptance criteria pass on the final run (or escalation is documented); idempotence run was performed and its outcome (clean no-op, or partial-state note for escalation) is recorded; iteration log captures any prompt changes; Rules.md untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment ↔ live wiki | This plan WRITES to `wiki/inbox/_session.md` and `wiki/RESEARCH/` (the alias probe). After the digest, the curator writes to `wiki/<CATEGORY>/` per its plan. All writes are inside the project's wiki/ tree. |
| Iteration loop ↔ curator/digest files | Task 3 may Edit `.claude/agents/wiki-curator.md` and `.claude/skills/digest/SKILL.md`. These edits are scoped to acceptance-criteria failures and bounded to 3 iterations. |
| Acceptance report ↔ git history | The report file is written under `.planning/phases/01-foundation-curator/`. It is an evidence artifact; subsequent phases reference it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Iteration loop drifts the curator prompt away from D-01..D-16 | mitigate | Iteration is bounded to 3 rounds and to specific failed criteria; the failure→fix mapping table in Task 3 keeps changes scoped. Each iteration appends to the report. |
| T-04-02 | Tampering | Test setup writes outside wiki/ | mitigate | Task 1's writes are explicitly to `wiki/inbox/_session.md` and `wiki/RESEARCH/`; verified by `files_modified` frontmatter. |
| T-04-03 | Tampering | Acceptance report contains injected content from curator output | accept | The report quotes the curator's plan output verbatim. The curator's output is itself bounded by Plan 02's prompt (path-safety, denylist, validation gate). |
| T-04-04 | Information disclosure | Test outputs include filed-note content | accept | All content is local-user-owned markdown; no PII or secrets. |
| T-04-05 | Spoofing | Idempotence verification can be faked by skipping the second run | mitigate | The acceptance gate's automated verify checks for the literal section "Idempotence run" plus `wc -l` evidence in the report; reviewer can git-blame the report's content vs the timestamped archive count. |

ASVS Level 1.
</threat_model>

<verification>
- Acceptance report exists with all sub-case results filled in (no `(?)` placeholders)
- Idempotence run section appended; second digest produced 0 file changes
- Archive file(s) exist in wiki/inbox/_archive/
- Rules.md unmodified across all runs (DIGS-13 holds)
- No `.planning/`, `.claude/`, or `src/` mutations beyond explicit iteration edits to the curator + digest skill
- All 15 phase requirements (DIGS-01..13, LIFE-02, LIFE-03) have empirical evidence in the report
</verification>

<success_criteria>
- Phase 1 success criteria from ROADMAP.md are mechanically verified, not asserted
- The acceptance report stands as audit evidence for downstream phases
- A future Claude reading this report can answer "what changed in Phase 1's iteration loop and why"
- Phase 2 can begin against a curator + digest skill that demonstrably handles the 5 fixture sub-cases
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-curator/01-04-SUMMARY.md` documenting:
- Final per-sub-case pass/fail (one line each)
- Number of iteration rounds (0 if first run passed clean)
- Specific prompt changes made during iteration (if any) and which criterion each addressed
- Total digest runs (initial + iterations + idempotence run)
- Time/cost characteristics observed (rough estimates of context consumed per digest run — informs whether the curator prompt is too verbose for production)
- Any acceptance criterion that the user opted to escalate rather than fix in Phase 1
</output>
