# Session Inbox

**Status**: live session state
**Purpose**: state-of-the-world mirror of what exists in the codebase this session. The codebase is ground truth; this file is a derived view.

---

@ ARCHITECTURE::digest-step-6-inbox-reset  •  .claude/skills/wiki-digest/SKILL.md  •  #digest #lifecycle #inbox
The wiki-digest skill body resets `wiki/inbox/_session.md` to the canonical empty template (header + Status + Purpose + `---`) as Step 6, after curator writes succeed and the post-write link audit completes. Filed entries no longer belong in the live inbox; the Step 2 archive preserves the pre-digest state. The reset is skipped if the curator aborted or the audit reported unrecoverable errors — the inbox is preserved so the user can retry.

@ RESEARCH::ripgrep-required-for-claude-code-tools  •  —  •  #prerequisite #ripgrep #tooling
Claude Code's `Glob` and `Grep` tools wrap `ripgrep` (`rg`) and fail with `posix_spawn 'rg' ENOENT` when `rg` is not on `$PATH`. Documented as a prerequisite in INSTALL.md (Prerequisites + Known interactions). Symptom during `/wiki-digest`: curator's same-concept conflict detection and post-write link audit lose access to filed-note enumeration via tools, falling back to the skill body's bash-injected `find`/`grep` outputs which work. Symptom during `/wiki-recall`: keyword grep across `wiki/` returns nothing useful. Fix: install ripgrep (e.g. `apt-get install ripgrep` on Debian/Ubuntu/WSL).

@ ARCHITECTURE::recall-skill-renamed-to-wiki-recall  •  .claude/skills/wiki-recall/SKILL.md  •  #skill #rename #convention
The recall skill is now `wiki-recall`; the slash command is `/wiki-recall`. Folder renamed via `git mv` to `.claude/skills/wiki-recall/`, SKILL.md frontmatter `name: wiki-recall`, the recall hook's wiki-only filter regex updated to match `/wiki-recall` (avoids recursive recall when the user invokes the slash command). The hook script filename remains `recall-prompt.sh` — internal scaffolding, not a slash command, so no rename. The previously-documented "recall is a wiki-prefix exception" rationale is removed: Claude Code's skill and agent registries are independent, so the `wiki-recall` skill and `wiki-recall` sub-agent coexist by name without collision (the skill registers via `name:` for slash commands; the skill's `agent: wiki-recall` field resolves the fork target via the agent registry).
