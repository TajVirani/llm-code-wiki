# Session Inbox

**Status**: live session state
**Purpose**: state-of-the-world mirror of what exists in the codebase this session. The codebase is ground truth; this file is a derived view.

---

@ ARCHITECTURE::version-1-1-0-release  •  CHANGELOG.md  •  #release #versioning #wiki #linking
Release 1.1.0 ships the piped wiki-link contract switch. CHANGELOG.md's 1.1.0 entry describes the bare-to-piped migration with a deterministic transform recipe (kebab-case basename derivation, code-block/HTML-comment skip, post-sweep audit via the curator's Step 8). Consumers running `/wiki-update` past 1.0.0 must run that sweep on their own wikis — `/wiki-update` does not touch `wiki/` content.

@ ARCHITECTURE::version-1-2-0-release  •  CHANGELOG.md  •  #release #versioning #modules
Release 1.2.0 ships the MODULES orientation layer. VERSION reads `1.2.0`; CHANGELOG.md's 1.2.0 entry enumerates Rules.md amendments (§2/§5/§11/§12/§13), the new template, curator deletion-test gate, trigger 7 signal contract, /wiki-modules skill, recall orienting/narrow split, optional inbox-update auto-handle, and topic-index H3 restructure. Migration is automatic — first MODULES write triggers the topic-index restructure; no consumer action required.

@ ARCHITECTURE::modules-category  •  wiki/Rules.md  •  #wiki #modules #orientation #architecture
MODULES is the sixth canonical category (Rules.md §2). Holds ~6–10 cluster summaries per project — orienting overviews of capability areas spanning multiple subsystems, each linking down to ARCHITECTURE/FUNCTIONS/RESEARCH/DIAGRAMS detail notes via H4 `#### From <CATEGORY>` sub-headings. Slugs are bare single-concept kebab identifiers (Rules.md §5). Same-concept detection uses a 1-of-2 rule (filename + title; tag overlap dropped because parent/child structurally share tags — Rules.md §13). Cross-category slug collisions surface as `SLUG-COLLISION` plan rows.

@ FUNCTIONS::wiki-modules-skill  •  .claude/skills/wiki-modules/SKILL.md  •  #skill #modules #audit
Read-only `/wiki-modules` slash command. Synth section proposes MODULES notes for clusters lacking one using three deterministic signals — filename-prefix grouping (≥3 members), tag-overlap cohesion (cluster mode + misfit detection), link-graph density (intra-cluster `[[wiki-link]]` edge count). Audit section checks every existing module for broken children, deprecated links, unlinked cluster candidates, and scope drift. Both sections emit in one run; no writes, no interactive prompts.

@ ARCHITECTURE::trigger-7-module-cluster  •  wiki/Rules.md  •  #curator #trigger #modules #signals
Rules.md §12 row 7 defines module-cluster shape detection by all four signals: S1 ≥3 wiki-link/basename references, S2 ≥3 of 4 keyword categories (trigger/storage/executor/outcome), S3 ≥2 distinct dominant domain tags (top-frequency from topic-index.md), S4 word band 150–1000. The wiki-digest skill body's Step 3a pre-evaluates all four in bash and emits `### Trigger 7 signals: S1=N, S2=[trig=N, store=N, exec=N, out=N], S3=N, S4=N` per entry/concept so the curator applies the all-four rule deterministically — no LLM judgment. `disableSkillShellExecution: true` falls back to curator-side Read/Grep computation.

@ ARCHITECTURE::deletion-test-gate  •  .claude/agents/wiki-curator.md  •  #curator #modules #validation
Curator Step 5a gates every CREATE/EDIT routed to MODULES against the inner H2 skeleton of `_templates/module.md`. Body must satisfy ≥5 of 7 H2s with `Purpose` AND `Boundary` mandatory plus ≥3 of {Triggers, Storage, Behavior, Rules & Invariants, Children}. "Satisfies" means heading present + at least one non-placeholder content line beneath it. Failures convert to surface-only `SHALLOW-MODULE` plan rows excluded from D-03 plan-level approval. The 5-of-7 threshold lives in the curator prompt — tunable without amending Rules.md.

@ ARCHITECTURE::topic-index-h3-split  •  wiki/topic-index.md  •  #topic-index #recall #modules
Topic-index now uses two H3 sections inside `## Content`: `### Modules` (orientation, `Module: MODULES/<slug>.md` form, one bullet per module file) above `### Notes` (detail, `Files: PATH1, PATH2` form). Children of MODULES stay listed in `### Notes` flatly — never removed when a parent module is added. Combined cap stays ≤100. Curator Step 9 detects pre-rollout indexes (no `### Modules` heading) and performs a one-time additive restructure on first MODULES write — bullets stay in order, no content moves. Recall agent prefers `### Modules` for orienting queries ("what is", "how does", "overview of"), `### Notes` for narrow ones.
